docs(plan): update LlmResponse shape in B3 caveat to reflect reality
scale: ~2 lines across 1 files [XS]

- docs/superpowers/plans/2026-05-30-effort-route.md: Corrected the `LlmResponse` shape in the B3 caveat from `usage.input_tokens`/`output_tokens` to `LlmResponse { content, prompt_tokens, completion_tokens }`. Clarified that `infer_with_retry` returns `ActivityResult::Ok(Ok((resp, _cfg)))` and that the routing layer derives `total_member_tokens` from `MeasuredCost`, not the LLM response.

--- audit: original='docs(plan): record real LlmResponse shape in B3 caveat' | actual=2L across 1 files | derived=full ---
