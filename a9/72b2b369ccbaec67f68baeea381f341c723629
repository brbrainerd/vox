ci(codeql, gitleaks): Update CodeQL Rust deps and switch Gitleaks to CLI
scale: ~42 lines across 2 files [S]

- **codeql.yml**:
    - Expand Rust system dependencies for CodeQL builds to include full GTK/WebKit stack (`libglib2.0-dev`, `libgtk-3-dev`, `libwebkit2gtk-4.1-dev`, `libsoup-3.0-dev`, `libjavascriptcoregtk-4.1-dev`, `libappindicator3-dev`, `librsvg2-dev`) in addition to `libdbus-1-dev` and `pkg-config`. This ensures `cargo build --workspace` succeeds when pulling Tauri GUI backend dependencies.
- **gitleaks.yml**:
    - Replace `gitleaks/gitleaks-action@v2` with a direct execution of the MIT-licensed Gitleaks CLI (v8.30.1).
    - Add steps to download and install the Gitleaks CLI.
    - Configure the Gitleaks scan to use `gitleaks git . --redact --verbose --exit-code 1`, ensuring the job fails on findings.

--- audit: original='ci: full GUI system deps for CodeQL build + run gitleaks via MIT CLI' | actual=42L across 2 files | derived=full ---
