# GENERATED by `vox ci policy-registry --write`. Do not hand-edit builtin entries.
# SSOT for the unified policy catalog.
schema_version: 1
policies:
- id: code-audit/ai-laziness
  domain: code-audit-rule
  title: AI Laziness Patterns
  group: Language rules / AI-laziness
  description: 'Catches subtle AI cop-outs: placeholder string returns, ''implement later'' comments, mock-named functions shipped to prod, custom-type default returns, conditional stubs, assertion-only bodies, and early-return-only bodies.'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#ai-laziness
    detail: |-
      // VIOLATION — AI laziness pattern: placeholder return or 'implement later' comment
      fn calculate_tax(amount: f64, region: &str) -> f64 {
          // TODO: implement tax calculation
          0.0  // placeholder
      }

      // FIX — real implementation; scope down if full implementation is out of scope
      fn calculate_tax(amount: f64, region: &str) -> f64 {
          let rate = TAX_RATES.get(region).copied().unwrap_or(0.0);
          (amount * rate * 100.0).round() / 100.0
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/empty_body
  domain: code-audit-rule
  title: Empty Body Detector
  group: Language rules / General
  description: Detects functions with empty or trivially-defaulted bodies
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/empty_body
    detail: |-
      // VIOLATION — empty function body does nothing
      fn on_user_created(user: User) {}

      // FIX — implement the handler or explicitly note it is intentional
      fn on_user_created(user: User) {
          audit_log.record(AuditEvent::UserCreated { user_id: user.id });
          welcome_email::send(&user);
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/god_object
  domain: code-audit-rule
  title: God Object Detector
  group: Language rules / General
  description: Detects files or structs that are too large and should be decomposed into traits or smaller modules.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/god_object
    detail: |-
      // VIOLATION — single struct with 600+ lines and 15+ methods (God Object)
      pub struct AppManager {
          // auth, payments, notifications, reporting, users, config ... all here
      }
      // (600 lines of mixed responsibilities)

      // FIX — split by responsibility into focused structs
      pub struct AuthManager { ... }      // only auth
      pub struct PaymentProcessor { ... } // only payments
      pub struct NotificationService { ... } // only notifications
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/organization
  domain: code-audit-rule
  title: File Organization Detector
  group: Language rules / General
  description: Identifies structural anti-patterns like bloated lib.rs files or unorganized type dumps.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/organization
    detail: |-
      // VIOLATION — lib.rs with many direct struct/enum/trait definitions
      // crates/my-crate/src/lib.rs contains:
      pub struct Config { ... }       // should be in config.rs
      pub enum Status { ... }         // should be in status.rs
      pub trait Processor { ... }     // should be in processor.rs
      pub fn calculate(...) { ... }   // should be in a submodule

      // FIX — lib.rs should only re-export from submodules
      // lib.rs:
      pub mod config;
      pub mod status;
      pub use config::Config;
      pub use status::Status;
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/schema_compliance
  domain: code-audit-rule
  title: Schema Compliance Detector
  group: Language rules / General
  description: Verifies that files are located in paths authorized by vox-schema.json.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/schema_compliance
    detail: |-
      // VIOLATION — file placed outside its authorized path_pattern in vox-schema.json
      // vox-schema.json declares:
      //   "vox-payments": { "path_pattern": "crates/vox-payments/src/**" }
      // but file is at: crates/shared/src/payment_handler.rs  ← wrong location!

      // FIX — move the file to its authorized location
      // crates/vox-payments/src/payment_handler.rs  ← matches path_pattern
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/sprawl
  domain: code-audit-rule
  title: Architectural Sprawl Detector
  group: Language rules / General
  description: Enforces clean directory structures and prevents 'junk drawer' modules like utils.rs.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/sprawl
    detail: |-
      // VIOLATION — generic 'junk drawer' module name
      // src/utils.rs  ← forbidden: too generic, no clear boundary
      // src/helpers.ts ← forbidden: catch-all for unrelated logic

      // FIX — name modules after what they actually do
      // src/date_formatting.rs  ← clear responsibility
      // src/currency_math.rs    ← clear responsibility
      // src/auth_tokens.rs      ← clear responsibility
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/stub
  domain: code-audit-rule
  title: Stub / Placeholder Detector
  group: Language rules / General
  description: Detects todo!(), unimplemented!(), pass stubs, and throw-not-implemented patterns
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/stub
    detail: |-
      // VIOLATION — unimplemented stub left in shipped code
      fn calculate_discount(price: f64) -> f64 {
          todo!()
      }

      // FIX — real implementation or scope down to what compiles
      fn calculate_discount(price: f64) -> f64 {
          price * 0.10
      }
  default_enabled: true
  protected: true
  origin: builtin
- id: code-audit/arch/unwired
  domain: code-audit-rule
  title: Unwired Module Detector
  group: Language rules / General
  description: Detects modules declared but never imported or referenced
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/unwired
    detail: |-
      // VIOLATION — module declared but never imported elsewhere
      // In lib.rs:
      mod analytics;  // declared — but nothing imports it!
      mod payments;   // declared and used ✓

      // FIX — either use the module or remove the declaration
      mod analytics;   // now used in routes
      pub use analytics::track_event;  // wired in
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/arch/workspace_drift
  domain: code-audit-rule
  title: Workspace Drift Detector
  group: Language rules / General
  description: Enforces `workspace = true` inheritance for all sub-crate dependencies (path and version) and detects unregistered orphan crates in the workspace root.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#arch/workspace_drift
    detail: |-
      # VIOLATION — sub-crate Cargo.toml using path/version directly (workspace drift)
      [dependencies]
      serde = { version = "1", features = ["derive"] }  # bad: duplicates root version
      tokio = { path = "../../tokio" }                   # bad: use workspace = true

      # FIX — inherit from workspace
      [dependencies]
      serde = { workspace = true }
      tokio = { workspace = true }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/control-flow/option-combinator-opportunity
  domain: code-audit-rule
  title: Option Combinator Opportunity Detector
  group: Language rules / General
  description: Detects `match` over an `Option` that can be replaced with `.map(...)` or `.unwrap_or(...)` combinators.
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#control-flow/option-combinator-opportunity
    detail: |-
      // VERBOSE — two-arm match over Option
      let display_name = match user.nickname {
          Some(n) => n.to_uppercase(),
          None => "Anonymous".to_string(),
      };

      // IDIOMATIC — use Option combinator
      let display_name = user.nickname
          .map(|n| n.to_uppercase())
          .unwrap_or_else(|| "Anonymous".to_string());
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/control-flow/question-mark-opportunity
  domain: code-audit-rule
  title: Question Mark Opportunity Detector
  group: Language rules / General
  description: Detects `match` over a `Result` that can be replaced with the `?` operator.
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#control-flow/question-mark-opportunity
    detail: |-
      // VERBOSE — manual match on Result for error propagation
      fn load_config(path: &str) -> Result<Config, Error> {
          let content = match std::fs::read_to_string(path) {
              Ok(s) => s,
              Err(e) => return Err(e.into()),
          };
          // ...
      }

      // IDIOMATIC — use the ? operator
      fn load_config(path: &str) -> Result<Config, Error> {
          let content = std::fs::read_to_string(path)?;
          // ...
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/corpus/training-ineligible-import
  domain: code-audit-rule
  title: Training Ineligible Import Detector
  group: Language rules / General
  description: 'Detects files marked `training_eligible: true` that import from archive, deprecated, or legacy module paths, which are likely ineligible for training data.'
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#corpus/training-ineligible-import
    detail: |-
      // VIOLATION — file is training_eligible but imports from a deprecated path
      // training_eligible: true
      use crate::legacy::payment_processor::LegacyProcessor;  // bad: legacy import
      use crate::deprecated::auth::OldAuthHandler;            // bad: deprecated import

      // FIX — either update imports to current paths or mark file ineligible
      // training_eligible: true
      use crate::payments::processor::Processor;  // current path
      use crate::auth::handler::AuthHandler;      // current path
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/cross-platform/line-endings
  domain: code-audit-rule
  title: Line ending (LF) policy
  group: Language rules / General
  description: Flags CR / CRLF in source files; align with `.gitattributes` and `vox ci line-endings`
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#cross-platform/line-endings
    detail: |-
      # VIOLATION — file has Windows CRLF line endings (\r\n)
      # Each line ends with \r\n instead of \n
      # This causes diff noise and may break Unix tooling

      # FIX — configure your editor/git to use LF endings
      # .gitattributes:
      * text=auto eol=lf
      # Or convert with: git add --renormalize .
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/decorator/position-mismatch
  domain: code-audit-rule
  title: Decorator Position Mismatch Detector
  group: Language rules / General
  description: Detects bare keywords where decorators are required, or redundant decorator+keyword pairs.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#decorator/position-mismatch
    detail: |-
      // VIOLATION — bare keyword where @ decorator is required
      durable fn process_order(id: str) {
          // ...
      }

      // FIX — prefix with @
      @durable fn process_order(id: str) {
          // ...
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/deprecated-usage
  domain: code-audit-rule
  title: Deprecated Usage Detector
  group: Language rules / General
  description: Flags @deprecated annotations and raw JSX attribute leakage in Vox source
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#deprecated-usage
    detail: |-
      // VIOLATION 1 — @deprecated annotation left in shipped code
      @deprecated
      fn old_payment_flow(amount: f64) { ... }

      // VIOLATION 2 — raw JSX attributes leak into Vox source
      <div className="card" onClick={handler}>

      // FIX 2 — use Vox-native attribute syntax
      <div class="card" on:click={handler}>
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/doc/missing-adr-citation
  domain: code-audit-rule
  title: Missing ADR Citation Detector
  group: Language rules / General
  description: Detects public functions in critical crates whose doc comments lack an ADR-NNN or TASK-N.M citation.
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#doc/missing-adr-citation
    detail: |-
      // VIOLATION — pub fn in critical crate with no ADR/TASK citation
      /// Executes a workflow activity step.
      pub fn execute_step(ctx: &Context, step: Step) -> Result<Output, Error> {
          // ...
      }

      // FIX — cite the ADR that governs this function's interface
      /// ADR-042: Executes a workflow activity step per the orchestration spec.
      /// See also: TASK-3.2 (activity execution contract).
      pub fn execute_step(ctx: &Context, step: Step) -> Result<Output, Error> {
          // ...
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/dry-violation
  domain: code-audit-rule
  title: DRY Violation Detector
  group: Language rules / General
  description: Detects near-duplicate code blocks within the same file
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#dry-violation
    detail: |-
      // VIOLATION — near-duplicate function bodies (copy-paste)
      fn validate_email(s: &str) -> bool {
          let s = s.trim();
          s.contains('@') && s.len() > 3 && !s.starts_with('@')
      }
      fn validate_login(s: &str) -> bool {
          let s = s.trim();
          s.contains('@') && s.len() > 3 && !s.starts_with('@')  // identical body!
      }

      // FIX — extract the shared logic
      fn is_valid_email_format(s: &str) -> bool {
          let s = s.trim();
          s.contains('@') && s.len() > 3 && !s.starts_with('@')
      }
      fn validate_email(s: &str) -> bool { is_valid_email_format(s) }
      fn validate_login(s: &str) -> bool { is_valid_email_format(s) }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/handler/panicking-builtin
  domain: code-audit-rule
  title: Panicking Builtin in Handler Detector
  group: Language rules / General
  description: Detects panicking builtins inside actor message handlers or workflow activities, where a panic would crash the entire actor or workflow.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#handler/panicking-builtin
    detail: |-
      // VIOLATION — .unwrap() inside @handler crashes the whole actor on None/Err
      @handler
      fn on_payment(msg: PaymentMsg) -> Result<(), Err> {
          let record = db.find(msg.id).unwrap();  // panics if not found → kills actor
          Ok(())
      }

      // FIX — propagate with ? so the handler returns Err gracefully
      @handler
      fn on_payment(msg: PaymentMsg) -> Result<(), DbError> {
          let record = db.find(msg.id)?;
          Ok(())
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/import/cycle
  domain: code-audit-rule
  title: Import Cycle Detector
  group: Language rules / General
  description: Detects circular `import` dependencies in Vox files. Self-imports are caught per-file; multi-file cycles require the batch runner.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#import/cycle
    detail: |-
      // a.vox — VIOLATION: file imports itself
      import "./a.vox"

      pub fn greet() to str {
          "hello"
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/magic-value
  domain: code-audit-rule
  title: Magic Value Detector
  group: Language rules / General
  description: Detects hardcoded ports, IPs, file paths, and database connection strings
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#magic-value
    detail: |-
      // VIOLATION — hardcoded port and database path
      let server = Server::bind("0.0.0.0:8080");
      let db = Database::open("/var/data/app.db");

      // FIX — read from config or environment
      let port: u16 = config.server_port;        // from Config struct
      let server = Server::bind(("0.0.0.0", port));
      let db_path = std::env::var("DB_PATH")?;  // from environment
      let db = Database::open(&db_path);
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/require/justification-prose-required
  domain: code-audit-rule
  title: Require Justification Prose Required Detector
  group: Language rules / General
  description: Detects complex `@require(...)` expressions that lack an adequate justification comment.
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#require/justification-prose-required
    detail: |-
      // VIOLATION — complex @require with no explanation of why
      @require(amount > 0 && amount <= 1_000_000 && currency != null && !user.frozen)
      fn process_payment(amount: f64, currency: str, user: User) { ... }

      // FIX — add a justification comment explaining the constraints
      @require(amount > 0 && amount <= 1_000_000 && currency != null && !user.frozen)
      // because: amount must be positive (no zero-value payments); 1M cap is the
      // regulatory limit; currency required by ISO-4217 schema; frozen users blocked
      // by compliance policy (see ADR-031).
      fn process_payment(amount: f64, currency: str, user: User) { ... }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/retired/capacitor
  domain: code-audit-rule
  title: Retired Capacitor Detector
  group: Language rules / General
  description: Detects retired `@capacitor/*` imports and `npx cap` CLI invocations.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#retired/capacitor
    detail: |-
      // VIOLATION — importing from retired @capacitor/* namespace
      import { Camera } from '@capacitor/camera';        // retired
      import { Filesystem } from '@capacitor/filesystem'; // retired

      // VIOLATION — using retired npx cap CLI
      // npx cap sync

      // FIX — use Tauri 2 equivalents
      import { camera } from '@tauri-apps/plugin-camera';      // current
      import { fs }     from '@tauri-apps/plugin-fs';          // current
      // Build: cargo tauri build   (not npx cap sync)
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/retired/crate-import
  domain: code-audit-rule
  title: Retired Crate Import Detector
  group: Language rules / General
  description: Detects references to retired Vox crate names (vox-ludus, vox-sherpa-transcribe → vox-tauri-stt).
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#retired/crate-import
    detail: |-
      // VIOLATION — importing a retired Vox crate
      use vox_ludus::achievement::Achievement;   // retired: vox-ludus
      use vox_sherpa_transcribe::Transcriber;    // retired: vox-sherpa-transcribe

      // FIX — use the canonical replacements
      use vox_gamify::achievement::Achievement;  // current: vox-gamify
      use vox_tauri_stt::Transcriber;            // current: vox-tauri-stt
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/retired/decorator-usage
  domain: code-audit-rule
  title: Retired Decorator Usage Detector
  group: Language rules / General
  description: Detects decorator and import forms retired per AGENTS.md §Retired Surfaces (LLM Guard).
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#retired/decorator-usage
    detail: |-
      // VIOLATION — retired @component fn syntax
      @component fn Counter() {
          return <div>0</div>
      }

      // FIX — use bare component declaration
      component Counter() {
          return <div>0</div>
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/retired/env-var
  domain: code-audit-rule
  title: Retired Env Var Detector
  group: Language rules / General
  description: Detects code-level references to retired TURSO_* env-var names; suggests VOX_DB_* canonical names.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#retired/env-var
    detail: |-
      // VIOLATION — reading from retired TURSO_* env-var names
      let db_url   = std::env::var("TURSO_URL")?;      // retired
      let db_token = std::env::var("VOX_TURSO_TOKEN")?; // retired

      // FIX — use the canonical VOX_DB_* names
      let db_url   = std::env::var("VOX_DB_URL")?;   // canonical
      let db_token = std::env::var("VOX_DB_TOKEN")?; // canonical
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/retired/memory-api
  domain: code-audit-rule
  title: Retired Memory API Detector
  group: Language rules / General
  description: Detects call-sites to retired `recall()` / `recall_async()` memory-API names.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#retired/memory-api
    detail: |-
      // VIOLATION — retired memory API call
      let fact = recall("user.preference.theme");

      // FIX — use MemoryManager
      let fact = memory_manager.lookup_fact_by_key("user.preference.theme").await?;
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/rust/expect-call
  domain: code-audit-rule
  title: ExpectCallDetector
  group: Language rules / General
  description: 'Heuristic: `.expect(...)` in Rust; skips common test paths and cfg(test) lines.'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#rust/expect-call
    detail: |-
      // VIOLATION — .expect() panics with a message at runtime
      fn load_config() -> Config {
          let data = std::fs::read_to_string("config.json")
              .expect("config.json must exist");  // panics in production!
          serde_json::from_str(&data).expect("invalid JSON")
      }

      // FIX — propagate errors with ?
      fn load_config() -> Result<Config, Error> {
          let data = std::fs::read_to_string("config.json")?;
          Ok(serde_json::from_str(&data)?)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/rust/panic-call
  domain: code-audit-rule
  title: PanicCallDetector
  group: Language rules / General
  description: 'Heuristic: `panic!(...)` in Rust; skips common test paths and cfg(test) lines.'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#rust/panic-call
    detail: |-
      // VIOLATION — explicit panic! crashes the process
      fn get_config_value(key: &str) -> String {
          match config.get(key) {
              Some(v) => v.clone(),
              None => panic!("missing required config key: {key}"),  // crashes!
          }
      }

      // FIX — return a Result instead
      fn get_config_value(key: &str) -> Result<String, ConfigError> {
          config.get(key)
              .cloned()
              .ok_or_else(|| ConfigError::MissingKey(key.to_string()))
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/rust/unwrap-call
  domain: code-audit-rule
  title: UnwrapCallDetector
  group: Language rules / General
  description: 'Heuristic: `.unwrap()` in Rust; skips common test paths and cfg(test) lines.'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#rust/unwrap-call
    detail: |-
      // VIOLATION — .unwrap() can panic at runtime
      fn get_user(id: UserId) -> User {
          db.users.find(id).unwrap()  // panics if user is None
      }

      // FIX — propagate the error or handle it explicitly
      fn get_user(id: UserId) -> Result<User, DbError> {
          db.users.find(id)?.ok_or(DbError::UserNotFound(id))
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/scaling/surfaces
  domain: code-audit-rule
  title: Scaling surfaces detector
  group: Language rules / Scaling
  description: 'Heuristics for scaling risks: blocking I/O in async, literals, regex, SQL/HTTP patterns'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#scaling/surfaces
    detail: |-
      // VIOLATION — blocking I/O call inside an async function
      async fn fetch_user_data(id: UserId) -> UserData {
          let raw = std::fs::read_to_string("/var/data/users.json") // blocking!
              .unwrap();
          parse_user(id, &raw)
      }

      // FIX — use async I/O
      async fn fetch_user_data(id: UserId) -> Result<UserData, Error> {
          let raw = tokio::fs::read_to_string("/var/data/users.json").await?;
          Ok(parse_user(id, &raw))
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/secret/leaked-to-span
  domain: code-audit-rule
  title: Secret Leaked to Span Detector
  group: Language rules / General
  description: Detects secret-shaped field names (password, token, api_key, …) appearing as tracing span attributes or log call arguments.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#secret/leaked-to-span
    detail: |-
      // VIOLATION — token value logged directly to a tracing span
      tracing::info!(user = %user_id, token = %auth_token, "request processed");

      // FIX — omit the secret field entirely, or use a redacted placeholder
      tracing::info!(user = %user_id, "request processed");  // omit token
      // or: tracing::info!(user = %user_id, token = "[REDACTED]", "request processed");
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/security/hardcoded-secret
  domain: code-audit-rule
  title: Hardcoded Secret Detector
  group: Language rules / General
  description: Detects potential API keys, passwords, and other credentials in source code.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#security/hardcoded-secret
    detail: |-
      // VIOLATION — hardcoded API key in source code
      fn connect() {
          let client = ApiClient::new("sk-live-abc123xyz789");  // NEVER hardcode secrets
      }

      // FIX — read from environment variable
      fn connect() -> Result<ApiClient, ConfigError> {
          let key = std::env::var("API_KEY")?;
          Ok(ApiClient::new(key))
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/skeleton/declared-not-called
  domain: code-audit-rule
  title: Integration Graph Reachability Detector
  group: Language rules / General
  description: 'Detects fake wiring: pub fns that return trivial defaults and are not called from tests or entry points.'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#skeleton/declared-not-called
    detail: |-
      // VIOLATION — pub fn declared but returns trivial default and is never called
      pub fn process_events(events: Vec<Event>) -> Vec<Result<(), Error>> {
          Vec::new()  // trivial default: not actually processing anything
      }
      // (no test or entry point ever calls process_events)

      // FIX — implement the function and wire it into a test
      pub fn process_events(events: Vec<Event>) -> Vec<Result<(), Error>> {
          events.into_iter().map(|e| handle_event(e)).collect()
      }
      #[test]
      fn test_process_events() { assert!(!process_events(sample()).is_empty()); }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/skeleton/hollow-fn
  domain: code-audit-rule
  title: Hollow Function Detector
  group: Language rules / General
  description: Detects functions with trivially-default return values (compile but do nothing)
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#skeleton/hollow-fn
    detail: |-
      // VIOLATION — hollow function: compiles but does nothing
      fn process_payment(amount: f64, user_id: UserId) -> Result<PaymentResult> {
          Ok(PaymentResult::default())  // structural skeleton only
      }

      // FIX — real implementation
      fn process_payment(amount: f64, user_id: UserId) -> Result<PaymentResult> {
          let account = accounts.get(user_id)?;
          account.charge(amount).map(PaymentResult::Success)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/skeleton/no-test-for-pub-fn
  domain: code-audit-rule
  title: No Test For Exported Fn
  group: Language rules / General
  description: Exported fn in examples/golden/ Vox file is not called from any @test block
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#skeleton/no-test-for-pub-fn
    detail: |-
      // VIOLATION — public fn with no @test annotation anywhere in the file
      pub fn parse_invoice(raw: str) -> Invoice {
          Invoice::from_json(raw)
      }

      // FIX — add a @test block
      pub fn parse_invoice(raw: str) -> Invoice {
          Invoice::from_json(raw)
      }

      @test
      fn test_parse_invoice_roundtrip() {
          let inv = parse_invoice('{"id":"1","amount":42}')
          assert(inv.amount == 42)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/skeleton/untested-pub-api
  domain: code-audit-rule
  title: Untested Public API
  group: Language rules / General
  description: 'Rust library file has pub fn declarations but no #[test] blocks'
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#skeleton/untested-pub-api
    detail: |-
      // VIOLATION — pub fn with no #[test] block in this file
      pub fn calculate_tax(amount: f64, rate: f64) -> f64 {
          amount * rate
      }

      // FIX — add an inline test module
      pub fn calculate_tax(amount: f64, rate: f64) -> f64 {
          amount * rate
      }

      #[cfg(test)]
      mod tests {
          use super::*;
          #[test]
          fn test_calculate_tax() {
              assert_eq!(calculate_tax(100.0, 0.1), 10.0);
          }
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/state-machine/unreachable-state
  domain: code-audit-rule
  title: State Machine Unreachable State Detector
  group: Language rules / General
  description: Detects states in `state_machine { }` blocks that have no outgoing `->` transition arrow — these states are terminal sinks and may indicate missing logic.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#state-machine/unreachable-state
    detail: |-
      // VIOLATION — Reviewing state has no outgoing -> transition (terminal sink)
      state_machine OrderFlow {
          Pending {
              on submit -> Reviewing
          }
          Reviewing {    // no -> transition: stuck here forever!
          }
      }

      // FIX — add transitions from Reviewing
      state_machine OrderFlow {
          Pending {
              on submit -> Reviewing
          }
          Reviewing {
              on approve -> Fulfilled
              on reject  -> Cancelled
          }
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/stringly-typed-enum
  domain: code-audit-rule
  title: Stringly-Typed Enum Detector
  group: Language rules / General
  description: Detects String fields with comments listing enum alternatives — should be a Vox ADT
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#stringly-typed-enum
    detail: |-
      // VIOLATION — stringly-typed field with enum alternatives in a comment
      struct OrderStatus {
          status: String,  // "pending" | "confirmed" | "shipped" | "delivered"
      }

      // FIX — use a proper ADT/enum
      enum OrderStatus {
          Pending,
          Confirmed,
          Shipped,
          Delivered,
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/style/duplicate-prefix-name
  domain: code-audit-rule
  title: Duplicate Prefix Name Detector
  group: Language rules / General
  description: Detects identifiers with a duplicated prefix segment such as `user_user_id`, likely a copy-paste error.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#style/duplicate-prefix-name
    detail: |-
      // VIOLATION — identifiers with duplicated prefix segments
      let user_user_id = req.user.id;   // bad: user_user_id
      let order_order_ref = order.ref;  // bad: order_order_ref

      // FIX — remove the duplicate prefix
      let user_id   = req.user.id;
      let order_ref = order.ref;
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/style/long-range-coupling
  domain: code-audit-rule
  title: Long Range Coupling Detector
  group: Language rules / General
  description: Detects `let` variable bindings whose last use is more than 80 lines from the declaration site — a sign of long-range coupling that makes code harder to follow.
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#style/long-range-coupling
    detail: |-
      // VIOLATION — connection_string declared on line 1, used 90 lines later
      let connection_string = build_db_url(&config);  // declared here
      // ... 90 lines of unrelated logic ...
      let conn = Database::connect(&connection_string);  // used 90 lines later!

      // FIX — narrow the scope or extract a helper function
      fn open_database(config: &Config) -> Result<Database, Error> {
          let connection_string = build_db_url(config);  // declared close to use
          Database::connect(&connection_string)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/syntax/version-mismatch
  domain: code-audit-rule
  title: Syntax Version Mismatch Detector
  group: Language rules / General
  description: Detects Vox source files with invalid `syntax_version` header format or conflicting version declarations.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#syntax/version-mismatch
    detail: |-
      // VIOLATION 1 — invalid version format (must be digits.digits)
      // syntax_version = "v0.6"   // bad: has 'v' prefix
      // syntax_version = "latest" // bad: not a version number

      // VIOLATION 2 — conflicting declarations in the same file
      // syntax_version = "0.5"
      // ... (more code) ...
      // syntax_version = "0.6"    // bad: conflicts with first declaration

      // FIX — single declaration at the top with correct format
      // syntax_version = "0.6"
      fn my_function() { ... }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/types/anonymous-error-type
  domain: code-audit-rule
  title: Anonymous Error Type Detector
  group: Language rules / General
  description: Detects functions returning `Result[T, str]` where a named error type should be used.
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#types/anonymous-error-type
    detail: |-
      // VIOLATION — anonymous error type (str) loses error context
      fn load_config(path: str) -> Result[Config, str] {
          parse_file(path).map_err(|e| e.to_string())
      }

      // FIX — named error type with meaningful variants
      enum ConfigError {
          NotFound(str),
          ParseFailure(str),
      }
      fn load_config(path: str) -> Result[Config, ConfigError] {
          parse_file(path).map_err(|e| ConfigError::ParseFailure(e.to_string()))
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/types/id-required-at-boundary
  domain: code-audit-rule
  title: ID Required at Boundary Detector
  group: Language rules / General
  description: Detects bare `str` parameters with `_id` suffix names at API boundaries (@query, @mutation, @server, @activity, actor message handlers) where typed ID wrappers should be used.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#types/id-required-at-boundary
    detail: |-
      // VIOLATION — bare str with _id suffix at an @query boundary
      @query
      fn get_order(order_id: str) -> Order {
          db.orders.get(order_id)
      }

      // FIX — use a typed ID wrapper
      @query
      fn get_order(order_id: Id[Order]) -> Order {
          db.orders.get(order_id)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/unresolved-ref
  domain: code-audit-rule
  title: Unresolved Reference Detector
  group: Language rules / General
  description: Detects function calls that don't appear to be defined or imported
  severity: info
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#unresolved-ref
    detail: |-
      // VIOLATION — calling a function that is never defined or imported
      fn process(data: Vec<u8>) -> Result<Output, Error> {
          let parsed = parse_frame(data)?;  // parse_frame not defined here!
          Ok(transform(parsed))
      }

      // FIX — define the function or add the import
      use crate::codec::parse_frame;  // bring it into scope
      fn process(data: Vec<u8>) -> Result<Output, Error> {
          let parsed = parse_frame(data)?;
          Ok(transform(parsed))
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/victory-claim
  domain: code-audit-rule
  title: Victory Claim / Leftover Marker Detector
  group: Language rules / Victory claims
  description: Detects premature 'Done!' comments, TODO/FIXME/HACK markers left behind
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#victory-claim
    detail: |-
      // VIOLATION — TODO/FIXME markers and premature completion comment
      fn apply_discount(price: f64, user: &User) -> f64 {
          // TODO: apply loyalty tier discount
          // HACK: just return full price for now
          price  // Done! (not actually done)
      }

      // FIX — implement fully and remove markers
      fn apply_discount(price: f64, user: &User) -> f64 {
          let rate = user.loyalty_tier.discount_rate();
          price * (1.0 - rate)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/vox/crypto/banned-crate-import
  domain: code-audit-rule
  title: Crypto Banned Crate Import Detector
  group: Language rules / General
  description: Detects imports or Cargo dependencies for banned cryptography crates (aegis, ring, aws-lc-rs, md5, sha1, openssl) that violate Vox cryptography policy.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#vox/crypto/banned-crate-import
    detail: |-
      # VIOLATION — banned crypto crate in Cargo.toml
      [dependencies]
      ring = "0.17"  # banned: drags in cmake/nasm, fails on Windows CI

      # FIX — use a pure-Rust crate or vox-crypto
      [dependencies]
      chacha20poly1305 = "0.10"
      # or: vox-crypto = { path = "crates/vox-crypto" }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/vox/effect/missing-net-decl
  domain: code-audit-rule
  title: Effect Missing Net Declaration Detector
  group: Language rules / General
  description: Detects public or endpoint functions in Vox files that perform HTTP or network calls without declaring `@uses(net)` on the function.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#vox/effect/missing-net-decl
    detail: |-
      // VIOLATION — pub fn calls http.get but lacks @uses(net)
      pub fn fetch_prices(symbol: str) -> List[Price] {
          http.get("/prices/" + symbol)  // network call not declared!
      }

      // FIX — add @uses(net) before the function
      @uses(net)
      pub fn fetch_prices(symbol: str) -> List[Price] {
          http.get("/prices/" + symbol)
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/vox/effect/pure-violated
  domain: code-audit-rule
  title: Pure Function Impure Call Detector
  group: Language rules / General
  description: Detects `@pure fn` declarations in Vox files that contain impure builtin calls such as HTTP, file I/O, database, random, logging, or concurrency primitives.
  severity: warn
  blocking: false
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#vox/effect/pure-violated
    detail: |-
      // VIOLATION — @pure fn contains an impure side-effect call
      @pure
      fn format_amount(amount: f64) -> str {
          log.info("formatting")  // IMPURE: logging is a side effect
          "$" + amount.to_string()
      }

      // FIX — remove the side-effect call
      @pure
      fn format_amount(amount: f64) -> str {
          "$" + amount.to_string()
      }
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/vox/llm/direct-provider-call
  domain: code-audit-rule
  title: LLM Direct Provider Call Detector
  group: Language rules / General
  description: Detects direct HTTP calls to known LLM provider hostnames that bypass populi.* builtins.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#vox/llm/direct-provider-call
    detail: |-
      // VIOLATION — direct call to LLM provider hostname
      let body = { model: "gpt-4", messages: [] }
      let resp = std.http.post_json("https://api.openai.com/v1/chat/completions", body)

      // FIX — use populi.* instead
      let resp = populi.complete("gpt-4", prompt)
  default_enabled: true
  protected: true
  origin: builtin
- id: code-audit/vox/secret/env-get-shape
  domain: code-audit-rule
  title: Env Secret Shape Detector
  group: Language rules / General
  description: Detects env.get / env::var calls whose argument looks like a secret-shaped variable name.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#vox/secret/env-get-shape
    detail: |-
      // VIOLATION — reading secret-shaped env var directly
      let api_key = env.get("OPENAI_API_KEY")

      // FIX — use the Clavis secret manager
      let api_key = vox_secrets::resolve_secret(SecretId::OpenAiApiKey)?
  default_enabled: true
  protected: false
  origin: builtin
- id: code-audit/vox/workflow/non-deterministic-builtin
  domain: code-audit-rule
  title: Workflow Non-Deterministic Builtin Detector
  group: Language rules / General
  description: Detects calls to non-deterministic builtins inside workflow blocks or workflow functions in Vox source files. Workflows must be deterministic for replay safety.
  severity: error
  blocking: true
  runs_on:
  - pre-commit
  - pre-push
  - ci
  source:
    kind: pattern
    ref: contracts/code-audit/rules.v1.yaml#vox/workflow/non-deterministic-builtin
    detail: |-
      // VIOLATION — time.now() inside a @workflow body
      @workflow fn process_order(id: str) {
          let started_at = time.now()  // non-deterministic!
      }

      // FIX — pass timestamp as input parameter
      @workflow fn process_order(id: str, started_at: str) {
          // use started_at from caller
      }
  default_enabled: true
  protected: false
  origin: builtin
