# CR-F2a — goldens whose codegen-rust (`--mode script`) output currently
# diverges from `// EXPECT:` (the interpreter's verified stdout). One rel-key
# per line, relative to examples/golden/. RATCHET: a NEW divergence fails
# `golden_arm_parity_test`. Fix the emit site, then REMOVE the line — never add.
# Seeded 2026-06-07 (codegen-rust 3/10). Full rustc error chains (2026-06-07,
# after first-layer fixes bc028e9160 + Result<T,E>):
#   range_and_indexing   — RUNTIME: .clone().push() pushes to a discarded clone (mutation-on-clone) -> index OOB
#   adt_multi_field      — 1× E0308 (CLOSE; multi-field ADT variant emit)
#   regex_free_functions — 3× E0308 (regex free-fn return/Option typing)
#   tuple_destructure    — 3× E0308
#   string_interpolation — needs string builtins: to_upper, split(&str not String),
#                          + ~13 E0308 (interpolant String coercion)
#   closures_hof         — needs Vec HOF emit (map/filter/fold/sorted_by_key as
#                          iterator chains) + Box<dyn Fn> clone (Rc/closure semantics)
#   json_as_typed        — needs JSON accessor emit (as_int/as_float→as_i64/as_f64)
#                          on serde_json::Value (32 errors)
# DEEP (missing builtin-method families, multi-PR): closures_hof, json_as_typed,
# string_interpolation. NEAR-GREEN (1-3 type fixes): range_and_indexing,
# adt_multi_field, regex_free_functions, tuple_destructure.
closures_hof.vox
json_as_typed.vox
string_interpolation.vox
