[package]
name = "repair-019-pure-calls-random"
version = "0.1.0"
kind = "library"
description = "Repair-corpus fixture: effect-violation bug class. @pure fn calls a helper that requires `uses random`."
