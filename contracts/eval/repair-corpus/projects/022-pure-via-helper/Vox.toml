[package]
name = "repair-022-pure-via-helper"
version = "0.1.0"
kind = "library"
description = "Repair-corpus fixture: effect-violation bug class. @pure fn calls a non-pure helper that declares `uses net`."
