// Repair-corpus fixture 008 — type-error bug class.
// The fn signature returns bool but the body returns an int (1 or 0).
// Expected repair: change the return literals to true/false, OR change
// the return type to int.

fn describe(flag: bool) to bool {
    if flag {
        return 1
    }
    return 0
}
