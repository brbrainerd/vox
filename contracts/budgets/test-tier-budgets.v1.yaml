# contracts/budgets/test-tier-budgets.v1.yaml
# Regenerate via: vox run scripts/perf/test-baseline.vox
schema_version: 1
generated_at: "2026-05-28"

# Budget semantics:
#   warn_ms: log a warning if a tier exceeds this
#   fail_ms: fail CI if a tier exceeds this
#
# Thresholds: warn = 1.2x measured, fail = 1.5x measured
#
# Tightened 2026-05-28 after the arch-check 24x speedup (164s -> 6.7s) and the
# coverage-run 10x speedup (263s -> ~27s) shipped. Prior budgets reflected the
# pre-fix baselines and were ~4-5x looser than necessary. See
# docs/superpowers/specs/2026-05-27-test-suite-baseline-measurements.md for the
# pre-fix baseline and contracts/reports/test-baseline-*.json for the post-fix
# measurements once the harness is re-run.

tiers:
  fast:
    # fmt (~0s) + ssot (~15s) + toestub (~26s) + arch-check (~7s) = ~48s
    measured_ms: 60000
    warn_ms: 72000          # 1.2x
    fail_ms: 90000          # 1.5x
    note: "Arch-check went from 165s to ~7s (commit 760dae75da et al.)"

  complete:
    # fast + clippy (~75s) = ~135s. Clippy is now the dominant fast-tier cost.
    measured_ms: 140000
    warn_ms: 168000
    fail_ms: 210000

  full:
    # complete + nextest workspace (~27s, slow tests included since #[ignore]
    # was removed) = ~165s
    measured_ms: 170000
    warn_ms: 204000
    fail_ms: 255000
    note: "Nextest workspace went from 170s to ~27s after arch-check fix"

  full_cov:
    # complete + llvm-cov nextest workspace (~27s) = ~165s. Coverage tax is now
    # negligible since arch-check's dominance is gone.
    measured_ms: 170000
    warn_ms: 204000
    fail_ms: 255000
    note: "Coverage run went from 263s to ~27s. With the Windows arg-length fix in scripts/perf/coverage-report.vox, this is now a viable local-loop tier."

  doctests:
    # Not re-measured this session; the doctest path was not touched.
    measured_ms: 51000
    warn_ms: 61000
    fail_ms: 77000
