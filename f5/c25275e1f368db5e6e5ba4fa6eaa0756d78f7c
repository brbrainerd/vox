feat(dashboard): port Command, Network, and Speak tabs to Vox
scale: ~291 lines across 3 files [L]

- `crates/vox-dashboard/app/src/tabs/command.vox`:
    - Implemented `DiagnosticRow`, `DiagnosticsPanel`, `TaskDispatch` components.
    - Defined `DiagnosticViewState` state machine (Collapsed, Expanded, Filtered).
    - Created `CommandTab` as the root tab with a panel switcher.
- `crates/vox-dashboard/app/src/tabs/network.vox`:
    - Implemented `NodeBadge` and `MeshLegend` components.
    - Defined `MeshViewState` state machine (Overview, NodeSelected).
    - Created `NetworkTab` as the root tab with a topology canvas placeholder.
- `crates/vox-dashboard/app/src/tabs/speak.vox`:
    - Implemented `ChatMessage` and `ComposerPanel` components.
    - Defined `ChatSessionState` state machine (Idle, Composing, Submitting).
    - Created `SpeakTab` as the root tab with session header and composer.

--- audit: original='feat(dashboard): TASK-7.2 complete — speak/command/network tabs ported to .vox' | actual=291L across 3 files | derived=full ---
