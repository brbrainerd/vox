feat(interop): Add Phase 5 React component imports and TS default import emit
scale: ~152 lines across 7 files [M]

- `crates/vox-compiler/src/parser/descent/decl/head.rs`: Implement parsing for `import react LocalName from "./Component.tsx"` syntax, handling `react` as a keyword for component imports and distinguishing it from `react.use_state` imports.
- `crates/vox-compiler/tests/parser_import_syntax_test.rs`: Add tests for the new React component import syntax, verifying correct lowering with ES module specifiers and coexistence with existing `react.dot` imports.
- `crates/vox-codegen/src/codegen_ts/reactive.rs`: Generate TypeScript `import <item> from "<spec>"` statements for external React components based on the new `es_module_specifier` field in `HirImport`.
- `crates/vox-compiler/src/hir/lower/mod.rs`: Update the HIR lowerer to handle the new `ImportPathKind::ReactComponent` variant, populating the `es_module_specifier` field in `HirImport`.
- `crates/vox-compiler/src/fmt/printer.rs`: Implement pretty-printing for the new `ImportPathKind::ReactComponent` syntax.
- `crates/vox-compiler/src/ast/decl/types.rs`: Introduce a new `ImportPathKind::ReactComponent` enum variant to represent React component imports, including `local_name` and `module_specifier`.
- `crates/vox-compiler/src/hir/nodes/decl.rs`: Add an optional `es_module_specifier` field to `HirImport` to store the ES module specifier for external React components.

--- audit: original='feat(interop): Phase 5 react component imports and TS default import emit' | actual=152L across 7 files | derived=full ---
