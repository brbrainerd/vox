feat(code-audit): migrate secret and magic value patterns to rule pack
scale: ~393 lines across 3 files [L]

- `crates/vox-code-audit/src/detectors/magic_value.rs`:
    - Replaced four `Regex` fields (`port_re`, `ip_localhost_re`, `abs_path_re`, `db_conn_re`) with `CompiledRule` references (`port_rule`, `ip_rule`, `path_rule`, `db_conn_rule`).
    - Updated `MagicValueDetector::new()` to load these rules using `pack_rule` from the embedded rule pack, specifically `magic-value/{port,ip,path,db-conn}`.
    - Removed the hardcoded regex patterns for these rules.
    - The `comment_re`, `const_def_re`, and `test_attr_re` regexes remain in Rust for structural filtering.
- `crates/vox-code-audit/src/detectors/secrets.rs`:
    - Replaced three `Regex` fields (`generic_secret`, `aws_key`, `jwt_token`) with `CompiledRule` references.
    - Updated `SecretDetector::new()` to load these rules using `pack_rule` from the embedded rule pack, specifically `security/hardcoded-secret/{generic,aws-key,jwt}`.
    - Removed the hardcoded regex patterns for these rules.
    - Removed the `supported_langs` field.
- `crates/vox-code-audit/tests/wave_c_parity.rs`: Added a new test file containing 12 tests to ensure parity for `SecretDetector` and `MagicValueDetector` after the rule migration. These tests verify correct detection and rule IDs for AWS keys, generic secrets, JWTs, and magic values (ports, IPs, paths, DB connections), including skip logic.

--- audit: original='feat(code-audit): wave-3 — secrets and magic_value patterns from rules.v1.yaml' | actual=393L across 3 files | derived=full ---
