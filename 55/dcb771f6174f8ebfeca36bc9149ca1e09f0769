feat(zenodo): Enrich deposit metadata with date, keywords, related identifiers, and version
scale: ~255 lines across 2 files [L]

- `crates/vox-publisher/src/zenodo_metadata.rs`:
    - Added `keywords_from_title` function to derive keywords from a title string, lowercasing, stripping non-alphanumeric characters, filtering by length, deduplicating, and capping at 8.
    - Modified `zenodo_deposition_create_body` to:
        - Parse `metadata_json` once for `scientia.*` keys.
        - Populate `publication_date` from `scientia.publication_date` or `manifest.created_at`.
        - Populate `keywords` from `scientia.keywords` or by deriving from `manifest.title` using `keywords_from_title`.
        - Populate `related_identifiers` from `scientia.related_identifiers`, `scientia.code_repository`, `scientia.data_repository`, `scientia.nanopub_uri`, and `scientia.swhid`.
        - Populate `version` from `scientia.version`.
- `crates/vox-publisher/src/zenodo_api_types.rs`:
    - Added `publication_date`, `keywords`, `related_identifiers`, and `version` fields to `ZenodoDepositionMetadata`, all marked as optional or with default serialization behavior.
    - Introduced `ZenodoRelatedIdentifier` struct to represent related identifiers with `identifier`, `relation`, and optional `resource_type` fields.

--- audit: original='feat(scientia): Zenodo metadata enrichment — date, keywords, related identifiers, version' | actual=255L across 2 files | derived=full ---
