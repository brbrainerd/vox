feat(compiler): allow `return`, `break`, `continue` as match-arm bodies
scale: ~53 lines across 4 files [M]

- `crates/vox-compiler/src/parser/descent/expr/pratt_match.rs`: Modified `parse_match` to peek for `Token::Return`, `Token::Break`, or `Token::Continue` before parsing a match arm's body. If one of these tokens is found, the parser now parses a single statement and wraps it in an `Expr::Block`. Otherwise, it proceeds with `parse_expr()`.
- `examples/golden/match_arm_stmts.vox`: Added a new golden file demonstrating match arms with `return` statements and multi-statement blocks.
- `docs/superpowers/plans/2026-05-08-language-features-tracker-index.md`: Updated the language features tracker to mark the "match-arm statement bodies" plan as landed in #74.
- `docs/superpowers/plans/2026-05-08-language-match-arm-statements.md`: Added an amendment to the plan document, noting that the implementation was simpler than initially suggested, primarily involving a parser change to handle statement keywords as `Expr::Block`s.

--- audit: original='feat(compiler): match-arm statement bodies (plan 3) (#74)' | actual=53L across 4 files | derived=full ---
