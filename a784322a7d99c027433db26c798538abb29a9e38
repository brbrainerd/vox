feat(lsp): gate `vox-lsp` db dependencies; add SBOM step to release workflow
scale: ~57 lines across 4 files [M]

- `crates/vox-lsp/Cargo.toml`:
    - Introduced a `db` feature, enabled by default, which makes `vox-db` and `vox-gamify` optional dependencies.
    - This allows `vox-lsp` to be built without these dependencies when `--no-default-features` is used.
- `crates/vox-lsp/src/main.rs`:
    - Conditioned the `Arc` and `OnceLock` imports, `LUDUS_PROJECT_DB` static, `ludus_lsp_events_disabled` function, `cached_project_db` function, and the `err_n`/`warn_n` diagnostic counts with `#[cfg(feature = "db")]`.
    - This ensures these components, which are related to the Ludus project-DB and telemetry, are only included when the `db` feature is enabled.
- `.github/workflows/release-binaries.yml`:
    - Added a new step to generate an SPDX SBOM using `anchore/sbom-action` for the `release-artifacts` directory.
    - Configured the SBOM generation with `continue-on-error: true` and `fail_on_unmatched_files: false` to prevent it from blocking releases.
    - The generated `sbom.spdx.json` is now included in the release assets.
- `docs/plans/INSTALL-RELEASE-AUDIT.md`:
    - Updated the status of "Phase 4.1b (gate `vox-lsp`)" to "✅" and provided details on the implementation and verification of the `db` feature gating.
    - Updated the status of "Release-workflow extension (SBOM)" to "🟡" and described the addition of the SBOM step, including its safeguards and the need for CI verification.
    - Added a note about a potential follow-on to add an `lsp` subcommand to `vox-langtool`.

--- audit: original='feat(lsp/release): gate vox-lsp db deps (Phase 4.1b) + SBOM step in release-binaries' | actual=57L across 4 files | derived=full ---
