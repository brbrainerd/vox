feat(cli): introduce `ai serve` and `populi eval-local` commands
scale: ~495 lines across 2 files [L]

- `crates/vox-cli/src/commands/ai/serve/mod.rs`: Add a new `ai serve` command for running a native inference server with an OpenAI-compatible API.
- `crates/vox-cli/src/commands/populi/eval_local.rs`: Add a new `populi eval-local` command to evaluate trained models against a heldout benchmark.

--- audit: original='fix(cli): use vox_corpus training system prompt in eval-local and serve' | actual=495L across 2 files | derived=full ---
