[package]
name = "vox-bounded-fs"
description = "Sandboxed, bounded filesystem operations from the Vox project."
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow = { workspace = true }
vox-scaling-policy = { workspace = true }

tokio = { workspace = true, optional = true }

[features]
default = []
async = ["dep:tokio"]

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
