// Each entry MUST cite why the violation is acceptable (non-shipped path /
// proven-not-a-lie). Empty is the goal: a growing allowlist is a smell.
export const HIDDEN_ALLOWLIST: { file: string; line: number; reason: string }[] = [];
