[package]
name = "vox-deploy-codegen"
description = "Deployment artifact codegen: Dockerfile, Compose, K8s, Fly, Coolify, systemd. Pure text generation."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
# D-9-rescope step 2: migrated from vox-container (L3) to vox-container-types (L0).
# Only pure types (BuildOpts, ContainerRuntime trait) are needed here; no runtime backends.
vox-container-types.workspace = true
vox-config.workspace = true
anyhow.workspace = true
thiserror.workspace = true
serde = { workspace = true }
serde_json.workspace = true
tracing.workspace = true
workspace-hack = { workspace = true }
