[package]
name = "vox-populi-types"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
publish = false
description = "Pure-data types for the Populi mesh: NodeRecord, PopuliRegistryFile, and helpers. L2 — no async runtime, no database."

[dependencies]
vox-mesh-types = { workspace = true }
vox-repository = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
workspace-hack = { workspace = true }
