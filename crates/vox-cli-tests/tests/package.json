{
  "name": "vox-cli-tests-tsc-harness",
  "version": "0.0.0",
  "private": true,
  "description": "Shared dependency manifest for `tsc --noEmit` checks against emitted vox output. The harness ensures `node_modules` here is populated (npm install runs once) and copies the tsconfig into each fixture's out dir so module resolution succeeds.",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-native": "^0.76.0",
    "react-native-safe-area-context": "^4.12.0",
    "expo-status-bar": "^2.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@tanstack/react-query": "^5.59.0",
    "@tanstack/react-router": "^1.87.0",
    "@tauri-apps/api": "^2.0.0",
    "zod": "^3.23.0"
  }
}
