# vox-scaling-policy

Bounded-resource scaling policy types extracted from the [Vox](https://github.com/vox-foundation/vox) project.

Provides compile-time and runtime accessors for the scaling SSOT (`contracts/scaling/policy.yaml`): concurrency limits, queue depths, memory budgets, and timeout policies used across the Vox workspace.

## License

Apache-2.0
