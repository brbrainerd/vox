//! Re-export shim — implementation lives in `vox-orchestrator-driver` (L3).

pub use vox_orchestrator_driver::{
    EmbeddedOrchestratorDriver, OrchestratorDriver, build_embedded_orchestrator_config,
};
