feat(scientia): add golden novelty harness with 13 labeled test cases
scale: ~562 lines across 2 files [L]

- `crates/vox-publisher/tests/novelty_golden_harness.rs`: Add a new golden harness test for `assess_novelty`. This test loads cases from `novelty_golden.v1.json`, runs them through `assess_novelty`, and asserts that the verdicts match the labeled expected classes. It also emits per-class precision/recall and includes specific assertions for conflict pairs.
- `crates/vox-publisher/tests/fixtures/novelty_golden.v1.json`: Add a new JSON fixture file containing 13 hand-crafted test cases for novelty assessment. These cases cover retrieval health, threshold boundaries (0.5/0.8 inclusive), chronological exclusion (future-dated and year-less hits), conflict pairs, and near-hit accumulation.

--- audit: original='test(scientia): golden novelty harness — 13 labeled cases, per-class precision/recall' | actual=562L across 2 files | derived=full ---
