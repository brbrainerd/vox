// A minimal async view demonstrating all four arms.
// vox:skip — requires web runtime for full execution
fn v(d: Async[Int]) -> Int {
    when d {
        fetching => 0
        empty => 0
        error e => -1
        ok x => x
    }
}
