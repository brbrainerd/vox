// ---
// title: "Web routing fullstack golden"
// description: "Nested routes { }, @query loader, pending, not_found, error — surfaces covered by routes.manifest.ts."
// syntax_version: "0.5.0"
// status: golden
// category: example
// constructs: [component, routes, @query, @loading, nested_routes]
// last_validated: 2026-04-07
// training_eligible: true
// training_weight: 1.0
// difficulty: advanced
// ---
// @training_prompt: Model a small app with nested routes, a @query route loader, pending UI, and not_found/error boundaries.

@query fn load_section() to int {
    return 1
}

@loading fn RoutePending() to Element {
    return text() { "wait" }
}

component Home() {
    state n: int = 0
    view: column() {
        text() { "home" }
        link(href="/section") { "Go to section" }
    }
}

component Section() {
    state n: int = 0
    view: text() { "section" }
}

component NotFoundPage() {
    state n: int = 0
    view: text() { "nf" }
}

component ErrorPage() {
    state n: int = 0
    view: text() { "err" }
}

routes {
    "/" to Home
    "/section" to Section
}
