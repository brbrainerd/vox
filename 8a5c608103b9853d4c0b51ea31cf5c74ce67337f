feat(vox-gui/e2e): extract Tauri-invoke mock to a reusable module
scale: ~539 lines across 2 files [L]

- `crates/vox-gui/ui/e2e/lib/tauriMock.ts`: New file created, containing the extracted `installTauriMock` function. This function sets `localStorage` items for `vox_active_view` and `vox_sidebar_mode`, and installs a rich `window.__TAURI_INTERNALS__.invoke` mock with predefined model data, queue snapshots, and other mock responses for various Tauri commands.
- `crates/vox-gui/ui/e2e/screenshots.spec.ts`: The `installMock` function, which contained the Tauri-invoke mock logic, was removed. The new `installTauriMock` function is now imported and used in its place.

--- audit: original='refactor(vox-gui/e2e): extract reusable Tauri-invoke mock to e2e/lib/tauriMock.ts' | actual=539L across 2 files | derived=full ---
