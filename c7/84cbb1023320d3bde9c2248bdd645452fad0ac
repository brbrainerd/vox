refactor(codegen): extract `FilterRecord` logic from `emit_db_table_op`
scale: ~364 lines across 2 files [L]

- `crates/vox-codegen/src/codegen_rust/emit/method_emit.rs`:
    - Extracted the `HirDbTableOp::FilterRecord` arm from `emit_db_table_op` into a new module-level function `emit_filter_record`.
    - Introduced new helper functions: `emit_filter_where_order_limit`, `emit_filter_where`, `build_filter_where_sql`, `emit_predicate_sql_module`, `single_param_pred`, and `combine_preds` to support `emit_filter_record`.
    - Moved the previously nested `emit_predicate_sql` to module scope as `emit_predicate_sql_module`.
    - Reduced the cyclomatic complexity of `emit_db_table_op` from 26 to 17.
- `contracts/reports/arch/cr-a1/2026-05-23.json`:
    - Updated the architectural report to reflect the change in `emit_db_table_op`'s complexity and the increased number of scanned functions (226 to 233).
    - Adjusted the `measured_at` timestamp.

--- audit: original='refactor(codegen): CR-A1 pass 7 — emit_db_table_op off the over-budget list' | actual=364L across 2 files | derived=full ---
