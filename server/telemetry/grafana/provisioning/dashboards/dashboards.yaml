apiVersion: 1

providers:
  - name: vox-telemetry
    orgId: 1
    folder: "Vox Telemetry"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
