docs(plans): outline codegen-ts bugs blocking tracker browser/Capacitor render
scale: ~157 lines across 1 files [M]

- Create `docs/superpowers/plans/2026-05-08-codegen-ts-bugs-blocking-tracker.md` to detail four TypeScript codegen bugs preventing the `vox-mental-tracker` app from running in a browser or Capacitor.
- Describe Bug A: `match` arms emit `case _:` literal patterns, causing bindings to vanish.
- Describe Bug B: `Speech.method()` lowers to `mobile.method()`, using the wrong runtime namespace.
- Describe Bug C: JSON-bearing string literals are not `JSON.stringify`'d, breaking emitted TS parsing due to inner double quotes.
- Describe Bug D: `@endpoint` calls and `std.*` references emit as bare identifiers without import statements.
- Explain that fixing these bugs is crucial for enabling the Playwright lane and Vite scaffold for the tracker app.

--- audit: original='docs(plans): codegen-ts bugs blocking tracker browser/Capacitor render (#78)' | actual=157L across 1 files | derived=full ---
