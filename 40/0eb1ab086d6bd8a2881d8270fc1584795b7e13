feat(share): wire Cloudflare backend and add checksum consistency test
scale: ~71 lines across 3 files [M]

- `crates/vox-share/tests/cloudflared_checksum_consistency_test.rs`: Add a new `#[ignore]` test to verify the hardcoded `cloudflared` checksums and version constant in `binary_cache.rs`. This test checks the version string format and ensures `cloudflared_url_and_checksum` returns a value for the current platform.
- `crates/vox-cli/src/commands/share.rs`:
    - Change the default value for the `--backend` argument from `lan` to `cloudflare`.
    - Remove the `anyhow::bail!` check that prevented using backends other than `lan`.
    - Add a new `--accept-tos` argument to accept Cloudflare's ToS without prompting.
    - Integrate `vox_share::consent::ensure_consent` for the `Cloudflare` backend.
    - Update the informational print statement to remove the `(S1) LAN backend` specific text.
- `crates/vox-share/src/coordinator.rs`:
    - Add `CloudflareBackend` to the `make_backend` function, allowing the `Cloudflare` backend to be instantiated.
    - Update the `unimplemented!` macro to reflect that `LocalhostRun` and `Tailscale` backends ship in later phases.

--- audit: original='feat(share): wire Cloudflare backend + checksum consistency test (S2 tasks 4-5)' | actual=71L across 3 files | derived=full ---
