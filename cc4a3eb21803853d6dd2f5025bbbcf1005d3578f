feat(codegen): map all Vox string methods to valid Rust in script mode
scale: ~532 lines across 5 files [L]

- **`crates/vox-codegen/src/codegen_rust/emit/method_emit.rs`**:
    - Extended `try_emit_str_method` to handle approximately 25 Vox string methods, ensuring they emit type-correct Rust code for `--mode script`.
    - Implemented Rust lowering for `len`, `is_empty`, `to_upper`/`to_uppercase`, `to_lower`/`to_lowercase`, `trim`, `trim_start`, `trim_end`, `contains`, `starts_with`, `ends_with`, `split` (0 or 1 arg), `replace`, `repeat`, `chars_count`, `count`, `is_alpha`, `is_digit`, `is_alnum`, `is_upper`, `is_lower`, `ord`, `chars`, `to_str`/`to_string`, `to_int`, and `to_float`.
    - Fixed E0716 (temporary-dropped-while-borrowed) in existing `slice`, `char_at`, and `index_of` implementations by introducing an owned bind (`__owned`) before taking a `&str` reference.
- **`crates/vox-codegen/tests/str_method_emit.rs`**: Added 30 new fast emit-to-string tests for Vox string-method lowering, covering the newly implemented methods and ensuring correct Rust string generation.
- **`crates/vox-codegen/tests/emit_compile_harness.rs`**: Added a new `str_methods_compile` ignored test case to verify that the full surface of Vox string methods compiles successfully when generated by the codegen path.
- **`crates/vox-codegen/tests/snapshots/main_boot_snapshot__main_boot_no_scheduled_no_endpoints.snap`**: Regenerated snapshot to include the `is_versioned: false` field in `HirFn` due to a drift in the `Hir` structure.
- **`crates/vox-codegen/tests/snapshots/main_boot_snapshot__main_boot_scheduled_actor_server.snap`**: Regenerated snapshot to include the `is_versioned: false` field in `HirFn` due to a drift in the `Hir` structure.

--- audit: original='feat(codegen): map all Vox string methods to valid Rust (--mode script) (#224)' | actual=532L across 5 files | derived=full ---
