test(codegen-ts): add golden snapshot tests for five features
scale: ~798 lines across 13 files [L]

- `crates/vox-codegen/tests/golden_ts_test.rs`: Add a new test file `golden_ts_test.rs` that reads `.vox` fixture files from `examples/golden-ts/`, parses them, lowers them to HIR, generates TypeScript code, and asserts the output against `insta` snapshots.
- `examples/golden-ts/form_basic.vox`: Add a new `.vox` fixture for a basic form with a required string field and an `on_submit` endpoint.
- `examples/golden-ts/safe_area.vox`: Add a new `.vox` fixture for a component utilizing `safe_area="top"`.
- `examples/golden-ts/back_button.vox`: Add a new `.vox` fixture for a back button with an `on_press` endpoint.
- `crates/vox-codegen/tests/snapshots/golden_ts_test__golden_ts_emit@form_basic.snap`: Add a new snapshot for the `form_basic.vox` fixture, including generated `forms.tsx`, `vox-app-contract.json`, and `vox-tanstack-query.tsx` files.
- `crates/vox-codegen/tests/snapshots/golden_ts_test__golden_ts_emit@endpoint_call_in_handler.snap`: Add a new snapshot for `endpoint_call_in_handler.vox`, including generated `C.tsx`, `vox-app-contract.json`, and `vox-tanstack-query.tsx` files.
- `crates/vox-codegen/tests/snapshots/golden_ts_test__golden_ts_emit@back_button.snap`: Add a new snapshot for `back_button.vox`, including generated `mobile.ts`, `vox-app-contract.json`, and `vox-tanstack-query.tsx` files.
- `crates/vox-codegen/tests/snapshots/golden_ts_test__golden_ts_emit@component_state.snap`: Add a new snapshot for `component_state.vox`, including generated `Counter.tsx`, `vox-app-contract.json`, and `vox-tanstack-query.tsx` files.
- `crates/vox-codegen/tests/snapshots/golden_ts_test__golden_ts_emit@safe_area.snap`: Add a new snapshot for `safe_area.vox`, including generated `SafeView.tsx`, `vox-app-contract.json`, and `vox-tanstack-query.tsx` files.
- `crates/vox-codegen/tests/snapshots/golden_ts_test__golden_ts_emit@simple_fn.snap`: Add a new snapshot for `simple_fn.vox`, including generated `vox-app-contract.json` and `vox-tanstack-query.tsx` files.
- `crates/vox-codegen/Cargo.toml`: Add `insta` as a dev-dependency.
- `Cargo.lock`: Update lockfile to include `insta`.
- `apps/vox-mental-tracker/package.json`: Update `package.json` (likely due to `Cargo.lock` changes or related build setup).

--- audit: original='test(codegen-ts): per-feature golden snapshot suite' | actual=798L across 13 files | derived=full ---
