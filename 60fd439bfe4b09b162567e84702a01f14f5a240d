fix(tests,contracts,grammar): resolve post-4843d7ce orphan failures
scale: ~1843 lines across 10 files [XL]

- contracts/workflow: Repair malformed JSON schema in `workflow-journal.v1.schema.json` by adding missing curly braces `{}` around object values, allowing `serde_json` parsing to succeed.
- crates/vox-workflow-runtime: Remove 1169 lines of `HirWorkflow`-dependent tests from `workflow_tracker_tests.rs` and `workflow/mod.rs`, replacing them with error-asserting stubs where `plan_workflow_activities` now returns `Err` when HIR is unavailable.
- crates/vox-integration-tests: Convert `activity` and `workflow` parse tests in `workflow_integration_test.rs`, `multimodal_image_gen_test.rs`, and `workflow_recovery_test.rs` to assert parse errors, reflecting the tombstoning of these keywords (TASK-2.6). Add `capabilities: vec![]` to `HirFn` initializer in `metamorphic_axiom_test.rs` (TASK-4.2 field).
- crates/vox-grammar-export: Update `BUILT_GRAMMAR_HASH` to the live SHA256 value in `versioning.rs`. In `export_test.rs`, fix `let_decl` to `let_stmt` and remove the non-existent `binary_expr` from `test_lark_known_constructs`.

--- audit: original='fix(tests,contracts,grammar): resolve all post-4843d7ce orphan failures' | actual=1843L across 10 files | derived=full ---
