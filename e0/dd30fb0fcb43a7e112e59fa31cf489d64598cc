feat(compiler): add segment-aware route pattern parser and overlap detection
scale: ~293 lines across 2 files [L]

- `crates/vox-compiler/src/codegen_ts/route_pattern.rs`: New module defining `Segment` enum (`Literal`, `Param`, `Wildcard`), `RoutePattern` struct, and `Overlap` enum. Implements `RoutePattern::parse` for converting path strings into `RoutePattern`s, handling redundant slashes, `:name` parameters, and `*` wildcards. Implements `RoutePattern::overlap_with` for detecting overlap between two patterns, returning `None`, `Shadowed`, or `Ambiguous` based on precedence rules.
- `crates/vox-compiler/src/codegen_ts/mod.rs`: Adds `route_pattern` as a public module.

--- audit: original='feat(compiler): add segment-aware RoutePattern overlap detection' | actual=293L across 2 files | derived=full ---
