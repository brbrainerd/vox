feat(vox-effort-audit): implement gix-backed commit walker
scale: ~340 lines across 3 files [L]

- `crates/vox-effort-audit/src/walk.rs`: Implement `iter_commits` function for traversing git commit history using `gix` for graph traversal and shelling out to `git diff` for diff details.
- `crates/vox-effort-audit/tests/support/mod.rs`: Add `make_smoke_repo` helper for creating temporary git repositories for testing.
- `crates/vox-effort-audit/tests/fixtures/repos/.gitkeep`: Add an empty `.gitkeep` file to preserve the directory structure.

--- audit: original='feat(vox-effort-audit): gix-backed commit walker (A7)' | actual=340L across 3 files | derived=full ---
