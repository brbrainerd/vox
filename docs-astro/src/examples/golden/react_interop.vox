// ---
// title: "Import and render an external React component"
// description: "Phase 5 interop — a named react import is rendered as a JSX tag inside a Vox component."
// syntax_version: "0.5.0"
// status: golden
// category: example
// constructs: [import, component]
// last_validated: 2026-06-03
// training_eligible: true
// training_weight: 1.0
// ---
// @training_prompt: Import an external React component into Vox and render it in a view
import react { Button } from "@acme/ui"

component Page() {
    view: column() {
        Button()
    }
}
