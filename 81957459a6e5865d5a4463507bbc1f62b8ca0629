chore(reports): update `measured_at` timestamps in daily artifact JSONs
scale: ~36 lines across 8 files [M]

- `contracts/reports/_snapshot/2026-05-22.json`: Updated `measured_at` timestamp.
- `contracts/reports/audit-all/2026-05-22.json`: Updated `measured_at` timestamp.
- `contracts/reports/aci-default/2026-05-22.json`: Updated `measured_at` timestamp.
- `contracts/reports/corpus-feedback/2026-05-22.json`: Updated `measured_at` timestamp.
- `contracts/reports/corpus-feedback/2026-Q2.json`: Updated `measured_at` timestamp.
- `contracts/reports/deploy/2026-05-22.json`: Updated `measured_at` timestamp.
- `contracts/reports/mens-on-distribution/2026-05-22.json`: Updated `measured_at` timestamp.
- `contracts/reports/retirement/2026-05-22.json`: Updated `measured_at` timestamp.

These changes reflect a re-run of `vox audit --gate all --strict-block-ga` for the 2026-05-22 readiness-snapshot document. The content (pass rates, thresholds, budgets) remains identical to the previous commit; only the `measured_at` timestamps have been refreshed. This ensures the snapshot document's "reproduction commands" appendix aligns with the files on disk.

--- audit: original='chore(reports): refresh daily artifacts from the snapshot audit pass' | actual=36L across 8 files | derived=full ---
