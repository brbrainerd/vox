feat(orchestrator-queue): persist op-log to vox-db with tiered retention
scale: ~539 lines across 12 files [L]

- `crates/vox-db`:
    - Bumps `BASELINE_VERSION` from 63 to 64 in `manifest.rs`.
    - Adds `convergence_op_log` and `convergence_op_log_backfill_dlq` DDL fragments to `vox_mesh.rs`.
    - Introduces `ops_convergence.rs` with `ConvergenceOpRow` and `insert_convergence_op_log` for CRUD operations on the new `convergence_op_log` table.
- `crates/vox-orchestrator-queue`:
    - Adds `persist.rs` for `OpLog` write-through persistence to `vox-db`, including `PersistContext` for daemon/set IDs and compaction interval.
    - Adds `checkpoint.rs` with `compact_now` stub for cold-tier compaction and pruning.
    - Extends `OperationKind` with a `Checkpoint` variant for cold-tier compaction, including `op_id_lo`, `op_id_hi`, `projection_blake3`, and `payload_blob_id`.
    - Adds `signature`, `signing_key_id`, `daemon_id`, and `parent_op_ids` fields to `OperationEntry`.
    - Modifies `OpLog` to include an optional `PersistContext` for database integration.
    - Updates `oplog/mod.rs` to export new persistence and checkpoint modules and `PersistError`.
    - Adds `oplog/store.rs` to support `OpLog::with_db` for database integration.
    - Updates `oplog/query.rs` to remove unused fields from `OperationEntry` reconstruction.
- `crates/vox-orchestrator-queue/tests`: Adds `oplog_persist.rs` with tests for `OpLog` persistence to `vox-db` and warm loading.

--- audit: original='feat(orchestrator-queue): persist op-log to vox-db with tiered retention (P3-T1)' | actual=539L across 12 files | derived=full ---
