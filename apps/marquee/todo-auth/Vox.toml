bin = []

[package]
name = "marquee-todo-auth"
version = "0.1.0"
kind = "application"
description = "Marquee Slot 2 — per-user todo list with bearer-token auth (CR-P1 / CR-L0 / CR-L7 fixture)"
license = "Apache-2.0"
authors = []
keywords = ["marquee", "todo", "auth", "single-tenant-saas"]
targets = []

[dependencies]

[dev-dependencies]

[features]

[skills]
