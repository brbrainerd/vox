chore(typeck): delete unused `infer` module
scale: ~440 lines across 2 files [L]

- `crates/vox-compiler/src/typeck/infer.rs`: Deleted the entire module, which contained legacy AST-based type inference logic, including functions like `lower_ast_ty`, `infer_expr`, `check_expr`, and `infer_stmt`. This module had no callers and was replaced by the `typecheck_hir` pipeline.
- `crates/vox-compiler/src/typeck/mod.rs`: Removed the `pub mod infer;` declaration, unexporting the deleted module.

--- audit: original='@ chore(typeck): delete dead infer.rs (legacy AST-based inference, 0 callers)' | actual=440L across 2 files | derived=full ---
