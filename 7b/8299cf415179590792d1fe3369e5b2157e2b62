refactor(vox-doc-pipeline): remove SUMMARY/feed generation, lint-only default
scale: ~845 lines across 11 files [L]

- `crates/vox-doc-pipeline/src/pipeline/summary.rs`: Deleted module responsible for `SUMMARY.md` generation.
- `crates/vox-doc-pipeline/src/pipeline/feed.rs`: Deleted module responsible for RSS `feed.xml` generation.
- `crates/vox-doc-pipeline/src/pipeline/mod.rs`:
    - Removed imports and calls to `feed::generate_feed` and `summary::{SECTION_ORDER, assert_summary_link_targets_unique, walk_dir}`.
    - Updated module-level documentation to reflect that `SUMMARY.md` and `feed.xml` are no longer generated by the pipeline.
    - Marked `--check` and `--lint-only` flags as legacy and ignored.
    - Reduced `run` function logic by removing summary and feed generation steps.
- `crates/vox-doc-pipeline/src/main.rs`:
    - Updated main documentation to reflect the pipeline's new role as a pure markdown linter.
    - Removed descriptions of `--check` and `--lint-only` modes as they are now ignored.
    - Added a note that `SUMMARY.md` and `feed.xml` are no longer generated by this crate.
- `crates/vox-doc-pipeline/src/lib.rs`: Updated crate-level documentation to reflect the change from a full documentation pipeline to a linter.
- `docs/src/architecture/*.md`: Corrected the `category` frontmatter from `"Architecture SSOTs"` to `"architecture"` in six architecture documents.

--- audit: original='refactor(vox-doc-pipeline): remove SUMMARY/feed/arch-index generation, lint-only default' | actual=845L across 11 files | derived=full ---
