feat(doc-pipeline): parallel linting and actionable error suggestions
scale: ~259 lines across 3 files [L]

- `crates/vox-doc-pipeline/src/pipeline/lint.rs`:
    - Made `VALID_CATEGORIES`, `VALID_STATUS`, and `VALID_SCHEMA_TYPES` public (`pub(crate)`).
    - Added `VALID_SCHEMA_TYPES` constant for supported schema.org types.
    - Implemented `suggest` function to provide "Did you mean X?" suggestions for invalid frontmatter values, prioritizing case-insensitive prefix matches over Levenshtein distance within a budget.
- `crates/vox-doc-pipeline/src/pipeline/mod.rs`:
    - Added `suggestion_hint` function to format the "Did you mean X?" message.
    - Added `workflow_for_kind` function to provide one-line remediation hints for each `LintKind`.
    - Modified `collect_lint_errors` and `collect_lint_errors_target` to use `rayon` for parallel processing of markdown files, collecting all errors in a single pass.
    - Updated error reporting to include actionable suggestions for `UnknownCategory`, `UnknownStatus`, and `UnknownSchemaType` errors.
    - Implemented a grouped summary of lint errors, counting each error class and emitting canonical remediation workflows.
- `crates/vox-doc-pipeline/Cargo.toml`: Added `rayon` dependency.

--- audit: original='feat(doc-pipeline): parallel lint + actionable error recommendations' | actual=259L across 3 files | derived=full ---
