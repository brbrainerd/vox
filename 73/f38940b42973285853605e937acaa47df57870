feat(tracker): implement local-first crash reporting with opt-in upload
scale: ~168 lines across 4 files [M]

- **apps/vox-mental-tracker/src/ErrorBoundary.tsx**: Add new React class component `ErrorBoundary` to catch unhandled errors, save them to an IndexedDB ring buffer named `vox-crashes` (max 50 entries), and provide functions to load and clear stored crashes.
- **apps/vox-mental-tracker/src/pages/SettingsPage.tsx**: Add new React component `SettingsPage` to display the count of locally stored crash reports and provide a button to opt-in to send them to developers via an API endpoint, then clear local storage.
- **apps/vox-mental-tracker/src/main.vox**:
  - Add a `CrashReport` `@table` definition with fields for `id`, `error`, `stack`, `ts`, and `route`.
  - Add a `record_crash_report` `@endpoint` (mutation) to receive crash report JSON.
  - Add a `SettingsPage` Vox component with a basic layout and a link to home.
  - Add a `/settings` route mapping to the `SettingsPage` component.
  - Add a "Settings" link to the `NavBar` component.
- **apps/vox-mental-tracker/src/main.tsx**: Wrap the main `App` component with the new `ErrorBoundary` to enable global error catching.

--- audit: original='feat(tracker): local-first crash reporting with opt-in upload' | actual=168L across 4 files | derived=full ---
