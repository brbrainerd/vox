feat(runtime-rn): Implement Expo config plugin for native library linking and permissions
scale: ~625 lines across 6 files [L]

- `clients/runtime-rn/plugin/index.js`: Add new Expo config plugin.
  - Copies `libvox_runtime_rn.so` per-ABI for Android and `vox_runtime_rn.xcframework` for iOS.
  - Adds feature-gated permissions to `AndroidManifest.xml` and `Info.plist`.
  - Registers Candle model asset paths via `assetBundlePatterns`.
  - Implemented as CommonJS due to Expo's plugin resolver.
- `clients/runtime-rn/plugin.js`: Add ESM shim for the CommonJS plugin.
- `clients/runtime-rn/app.plugin.js`: Add Expo config-plugin convention entry point, re-exporting the plugin.
- `clients/runtime-rn/tests/plugin.test.mjs`: Add 22 unit tests for the Expo config plugin, covering prop validation, permission additions, and asset pattern generation.
- `clients/runtime-rn/package.json`:
  - Update `exports` to include `./plugin`, `./plugin.js`, and `./app.plugin.js`.
  - Add `plugin`, `plugin.js`, and `app.plugin.js` to `files`.
  - Add `test` script to run `tests/plugin.test.mjs`.
  - Add `@expo/config-plugins` as an optional peer dependency.
- `clients/runtime-rn/plugin/package.json`: Add `package.json` to define `plugin/` as `commonjs` module type.

--- audit: original='feat(runtime-rn): Expo config plugin — native lib linking, gated permissions, model assets' | actual=625L across 6 files | derived=full ---
