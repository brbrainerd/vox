# Transitional allowlist for `vox ci sql-surface-guard`
#
# Policy: domain SQL belongs in `crates/vox-db/src/store/ops_*.rs` (see database-nomenclature.md).
# Built-in allowlist always includes: `crates/vox-db/`, `crates/vox-compiler/`.
#
# Add a `crates/<crate>/` line only while migrating that crate off
# `.connection().query|execute(` until the transitional entry can be removed.
crates/vox-secrets/
# vox-gamify uses db.connection() directly because the Rust orphan rule prevents
# adding impl VoxDb methods for gamification types in vox-db (types defined in vox-gamify).
# See commit 64299ea: consolidate ops_ludus SQL into vox-gamify.
crates/vox-gamify/
# vox-codegen emits .connection().query/execute strings as code-generation output,
# not as live SQL calls. The guard regex false-positives on these string literals.
crates/vox-codegen/src/codegen_rust/emit/
# vox-scientia producers aggregate raw benchmark/telemetry data by reading
# across table joins that have no single-model ops_* equivalent yet. Migrate
# to typed ops once the scientia store layer (vox-db ops_scientia.rs) is added.
crates/vox-scientia/src/producers/
# vox-workflow-runtime integration tests seed and verify DB state directly.
# Test-only usage; production code paths go through the typed ops layer.
crates/vox-workflow-runtime/tests/
# vox-sql is the sanctioned app-plane SQL backend surface. It must call
# backend connection/query/execute APIs by design while dialect-agnostic query
# builders and codegen integration are landing.
crates/vox-sql/
