{
  "version": "1.0",
  "updated_at": "2026-08-22T00:00:00Z",
  "keywords": [
    {
      "name": "fn",
      "example": "fn add(a: int, b: int) -> int { a + b }"
    },
    {
      "name": "let",
      "example": "let x = 10"
    },
    {
      "name": "match",
      "example": "match x { 10 => print(\"ten\"), _ => print(\"other\") }"
    },
    {
      "name": "return",
      "example": "return x"
    }
  ],
  "decorators": [
    {
      "name": "@pure",
      "example": "@pure fn checksum(payload: bytes) { ... }"
    },
    {
      "name": "@uses",
      "example": "@uses(net) fn fetch_remote() { ... }"
    },
    {
      "name": "@auth",
      "example": "@auth(scheme: bearer) table Task { ... }"
    },
    {
      "name": "@durable",
      "example": "@durable fn run_pipeline() { ... }"
    },
    {
      "name": "@scheduled",
      "example": "@scheduled(\"0 9 * * *\") fn nightly() { ... }"
    },
    {
      "name": "@test",
      "example": "@test fn adds_two_numbers() { ... }"
    },
    {
      "name": "@deprecated",
      "example": "@deprecated fn old_api() { ... }"
    }
  ],
  "builtins": [
    {
      "name": "print",
      "example": "print(\"Hello Vox\")"
    },
    {
      "name": "len",
      "example": "let l = len(my_list)"
    },
    {
      "name": "now",
      "example": "let t = now()"
    }
  ],
  "data_layer_keywords": {
    "note": "Bare-keyword declarations, NOT decorators. The @-prefixed spellings (@table, @query, @mutation, @server, @tool, @resource, @form, @index) became hard parse errors on 2026-06-30 (cd7cc96874).",
    "examples": [
      "table User { id: int, name: str, active: bool }",
      "query user_count() to int { return len(db.User.all()) }",
      "mutation seed_user(name: str) to str { db.User.insert({ name: name, active: true })\n    return \"created\" }",
      "server sync_now() to bool { return true }"
    ]
  }
}
