---
title: "Hard-Error Flip — Migration Manifest (P0 follow-up)"
date: 2026-06-29
status: ready-to-execute
program: core-surface-taxonomy
source: "Generated by the flip-codemod-manifest workflow (11 buckets, 613 findings)."
---

# Hard-Error Flip — Migration Manifest

Makes the deferred hard-error flip (the 7 Tier-1 -decorators become hard parse errors; the soft-keyword forms become canonical) **executable**. Classified from 613 occurrences across 11 buckets.

**Counts:** MIGRATE 107 edits (24 files) · KEEP 150 (do NOT touch) · SPECIAL 54

## Execution sequence (from RISKS)

- The single load-bearing logic change is parser/descent/mod.rs (arms at ~665-696, 866-868, 915-917): flipping warn_retired_decorator from ParseSeverity::Warning to Error. Every MIGRATE edit only works AFTER this flip; sequence the source migrations and the flip together or all parse_str/.expect() tests will panic mid-rollout.
- Scope discipline: only the seven Tier-1 heads flip (@table/@index/@query/@mutation/@server/@tool/@resource). @mcp.tool, @mcp.resource, @public, @auth, @webhook, @cors, @pii, @uses, @layer, @traced, @versioned, @tracked, @form, @get, @test, @scheduled, @back_button, @deep_link, @push, @place, @component remain valid @-decorators and must NOT be touched. Several MIGRATE edits sit next to these (e.g. @public @server, @auth @server, @query @uses) where only the Tier-1 head moves.
- Decorator ordering with surviving prefixes: for @public/@auth + @server etc., the keyword head must come AFTER the remaining @-decorators (e.g. '@public server ...', '@auth(...) server ...'). Verify the grammar accepts a surviving decorator immediately preceding a soft keyword, and the @query @uses(net) ordering (query @uses(net) name) parses.
- @table heads drop BOTH the @ and the trailing 'type'; @server/@query/@mutation/@tool heads drop @ and 'fn'; @index drops only @. Mixing these up (e.g. leaving 'type'/'fn') reintroduces parse errors. Parenthesized config must be preserved on the keyword form: table(pk: email) User {...}, table(extern, source: ...) User {...}.
- printer.rs:92 and fmt/mod.rs:198 are a coupled pair — change both or the fmt round-trip test fails. The fmt idempotency tests (mod.rs 101/112/121/130/191) also depend on the printer emitting the keyword form.
- Snapshot/insta tests need rebaselining after source migration: axum_emit_contract_test.rs:73 and main_boot_snapshot.rs (and any tauri_convergence_snapshots). Run `cargo insta review` after edits.
- Heuristic/detector SPECIALs (check.rs is_script_like; extract_vox/part_helpers.rs; transplant_pairs.rs; infer_vox_category) must AUGMENT not REPLACE: keep matching legacy @-form (external/legacy .vox files still exist) while adding keyword-form detection. Naive substring match on bare 'query'/'table' risks false positives on ordinary words inside scripts — use line-prefix/token-aware matching.
- keyword_decorator_equivalence.rs is the highest-churn SPECIAL file: the ast_eq harness, soft_keyword_recognized_in_script_mode, and tier1_decorators_still_parse_during_warning_first all assert the @ form parses. Their contract INVERTS at the flip (must become is_err()/error-payload assertions). Only line 153 (ident_uses_preserved) is a plain MIGRATE; lines 216 (@mcp.resource) stays untouched.
- lexer/cursor.rs tests (177, 253) and parser/descent/tests.rs:28 assert specific token/warning behavior that the flip removes — these need genuine rewrites, not respells. Keep cursor.rs @traced/@versioned/@tracked tests (lines 91, 78) unchanged.
- lexer/token.rs #[token("@x")] definitions and Display impls are KEEP — the lexer must still recognize the @ literals so the parser can report them as hard errors with a replacement payload. Do not delete these tokens as part of the flip.
- vox-corpus parse-verified templates (part_02.rs, part_03.rs, part_walk.rs SAMPLE_VOX) silently return None / fail under the parser-verify and ast-extract features; ensure those features are enabled in CI so the migrations are actually exercised. Distinguish emitted-source templates (MIGRATE) from prompt/label prose (KEEP) on adjacent lines.
- Training-answer corpus (rust_to_vox.rs:69, _web_pairs.inc snippets, multiturn.rs base_code) defines the canonical output the model is trained to emit — migrating these changes model behavior; the 'already_decorated' test name/semantics in multiturn.rs no longer hold since keyword form is not a decorator.

## SPECIAL items (judgment required)

### crates/vox-cli/src/commands/check.rs
- L31: is_script_like detection: app_markers / source.contains("@query") heuristic. Post-flip keyword-form app files use bare keywords, so substring on '@query' stops detecting. Rework to token/line-prefix based detection of bare query/mutation/server/table to avoid false positives on common words. Covers lines 31,32,33,35.
- L32: @mutation app_markers detector; part of is_script_like rework (see line 31).
- L33: @server app_markers detector; part of is_script_like rework (see line 31).
- L35: @table app_markers detector; part of is_script_like rework (see line 31).

### crates/vox-compiler/src/fmt/mod.rs
- L198: Assertion out.contains("@table type User {") pins printer output. Change to out.contains("table User {") in lockstep with printer.rs:92.

### crates/vox-compiler/src/fmt/printer.rs
- L92: Printer EMITS the canonical formatted output. Change self.out.push_str("@table type ") -> self.out.push_str("table ") (drop 'type'). Paired with fmt/mod.rs round-trip assertions.

### crates/vox-compiler/src/hir/validate.rs
- L110: correction_hint string advertises @table syntax: '...e.g. @table User { ... }' -> '...e.g. table User { ... }' so it teaches the keyword form, not the now-erroring @ form.
- L163: correction_hint: 'e.g. @index MyTable.idx_name on (field)' -> 'e.g. index MyTable.idx_name on (field)'.

### crates/vox-compiler/src/lexer/cursor.rs
- L177: test_query_and_mutation_lex_as_distinct_tokens asserts AtQuery/AtMutation/AtServer tokens. Post-flip rewrite: @query/@mutation/@server hard-error or produce keyword tokens. Do NOT silently respell.
- L253: test_decorators lexes '@component @tool @mcp.tool @resource @mcp.resource @place'. @tool/@resource flip; split assertions so non-flipped decorators stay and flipped ones test keyword/error path.

### crates/vox-compiler/src/parser/descent/mod.rs
- L666: warn_retired_decorator("@query",...) dispatch arm. Flip arms at lines 665-696, 866-868, 915-917 (AtQuery/AtMutation/AtServer/AtTool/AtResource/AtIndex/AtTable) to emit ParseSeverity::Error instead of Warning. The '@x'->'x' replacement data is unchanged. THIS IS THE CORE LOGIC CHANGE.
- L669: Token::AtMutation arm: flip to hard error.
- L677: Token::AtServer arm: flip to hard error.
- L684: Token::AtTool arm: flip to hard error.
- L689: Token::AtResource arm: flip to hard error.
- L867: Token::AtIndex arm: flip to hard error.
- L916: Token::AtTable arm: flip to hard error.

### crates/vox-compiler/src/parser/descent/tests.rs
- L28: retired_table_decorator_emits_warning_payload asserts @table parses with only a Warning. Rewrite to assert ParseSeverity::Error (Tombstoned) carrying @table->table replacement.

### crates/vox-compiler/tests/keyword_decorator_equivalence.rs
- L50: ast_eq harness: first arg is the @table decorator form fed to parse(...).expect. Post-flip it stops parsing. Redesign: assert @ form ERRORS or drop the decorator side. Applies to lines 50,56,64,72,80,88,96,107,115,127.
- L56: ast_eq @table(pk: uid) decorator arg breaks at flip (see line 50).
- L64: ast_eq @index decorator arg breaks at flip (see line 50).
- L72: ast_eq @query decorator arg breaks at flip (see line 50).
- L80: ast_eq @mutation decorator arg breaks at flip (see line 50).
- L88: ast_eq @server decorator arg breaks at flip (see line 50).
- L96: ast_eq @tool decorator arg breaks at flip (see line 50).
- L107: ast_eq @tool "web" decorator arg breaks at flip (see line 50).
- L115: ast_eq @resource("u","d") decorator arg breaks at flip (see line 50).
- L127: soft_keyword_recognized_in_script_mode: parse_script(lex("@query fn c()")).expect. Decorator side stops parsing at flip; redesign or drop decorator comparison.
- L228: tier1_decorators_still_parse_during_warning_first ASSERTS @ forms still parse. At flip, change parse(lex(src)).unwrap() over @ forms (lines 228-234) to assert is_err() with machine-readable replacement payload.
- L229: warning-first @index entry inverts to is_err() (see line 228).
- L230: warning-first @query entry inverts to is_err() (see line 228).
- L231: warning-first @mutation entry inverts to is_err() (see line 228).
- L232: warning-first @server entry inverts to is_err() (see line 228).
- L233: warning-first @tool entry inverts to is_err() (see line 228).
- L234: warning-first @resource entry inverts to is_err() (see line 228).

### crates/vox-corpus/src/corpus/extract_vox/part_helpers.rs
- L47: infer_vox_category content_lower.contains("@table"). KEEP @table check (legacy files) AND add keyword-form detection (line starts with 'table '). Do not delete.
- L53: infer_vox_category contains("@query"); keep and add keyword 'query' branch.
- L56: infer_vox_category contains("@mutation"); keep and add keyword 'mutation' branch.
- L101: extract_construct_blocks_heuristic starts_with("@table"); keep and OR starts_with("table "). Verify extract_vox_type_name handles 'table X {' (no 'type').
- L113: starts_with("@query"); keep and add starts_with("query ") reading name after 'query ' (no 'fn').
- L116: starts_with("@mutation"); keep and add starts_with("mutation ") reading name after 'mutation ' (no 'fn').

### crates/vox-corpus/src/synthetic_gen/transplant_pairs.rs
- L45: target_code.contains("@table type ") gates transplant-pair generation. Keep legacy match and OR contains("table ") so keyword-form targets still qualify.

### crates/vox-grammar-export/src/compact_prompt.rs
- L78: compact_prompt exporter emits LLM-facing syntax examples: '@server fn name(params) to RetType {...}' -> keyword form 'server name(params) to RetType {...}' (drop 'fn'). Decide whether to retain a legacy note.

### crates/vox-grammar-export/src/ebnf.rs
- L68: EBNF exporter table production: 'table = {decorator}, "@table", ["type"], ident,...' -> 'table = {decorator}, "table", ident,...' (drop optional 'type'). Decision: document post-flip keyword grammar.
- L69: EBNF index production: "@index" -> "index".
- L73: EBNF server_fn: 'server_fn = "@server", "fn", ident,...' -> 'server_fn = "server", ident,...' (drop 'fn').
- L74: EBNF query_fn: "@query","fn" -> "query" (drop 'fn').
- L75: EBNF mutation_fn: "@mutation","fn" -> "mutation" (drop 'fn').

### crates/vox-grammar-export/src/lark.rs
- L59: Lark table rule: 'table: decorator* "@table" ["type"] IDENT...' -> 'table: decorator* "table" IDENT...'.
- L60: Lark index rule: "@index" -> "index".
- L64: Lark server_fn: "@server" "fn" -> "server" (drop 'fn').
- L66: Lark query_fn: "@query" "fn" -> "query" (drop 'fn').
- L68: Lark mutation_fn: "@mutation" "fn" -> "mutation" (drop 'fn').

### crates/vox-grammar-export/tests/export_test.rs
- L18: Assertion ebnf.contains("@table"). If exporter emits keyword form (ebnf.rs/lark.rs below), change to expect keyword-form production; tie to exporter decision.

## MIGRATE edits (mechanical, apply AFTER the parser arms flip to Error)

### crates/vox-cli/src/commands/compile.rs (1)
- L471: `@query fn ping() uses net to int { return 1 } -> query ping() uses net to int { return 1 }`

### crates/vox-cli/src/commands/db/mod.rs (5)
- L111: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L112: `@query fn q1() to int { -> query q1() to int {`
- L131: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L132: `@query fn qa() to int { return len(db.User.all()) } -> query qa() to int { return len(db.User.all()) }`
- L133: `@query fn qb() to int { return len(db.User.filter({ active: true })) } -> query qb() to int { return len(db.User.filter({ active: true })) }`

### crates/vox-cli/tests/db_migrate_semantics_test.rs (1)
- L5: `@table type Task { title: str }\n -> table Task { title: str }\n`

### crates/vox-codegen/src/codegen_rust/emit/http.rs (1)
- L921: `@query fn health() to str { -> query health() to str {`

### crates/vox-codegen/tests/back_button_test.rs (3)
- L24: `@query fn handle_back( -> query handle_back(`
- L41: `@query fn handle_back( -> query handle_back(`
- L42: `@mutation fn navigate_home( -> mutation navigate_home(`

### crates/vox-codegen/tests/component_import_refs_test.rs (3)
- L42: `@query fn load_count( -> query load_count(`
- L89: `@mutation fn record_event( -> mutation record_event(`
- L117: `@query fn refresh( -> query refresh(`

### crates/vox-codegen/tests/deep_link_test.rs (3)
- L24: `@query fn handle_link( -> query handle_link(`
- L50: `@query fn handle_back( -> query handle_back(`
- L51: `@query fn handle_link( -> query handle_link(`

### crates/vox-codegen/tests/emit_main_includes_durable_boot.rs (3)
- L27: `@server -> server (decorator line preceding fn)`
- L78: `@server -> server`
- L104: `@table type Task { -> table Task {`

### crates/vox-codegen/tests/form_emit_test.rs (3)
- L19: `@mutation fn save_mood( -> mutation save_mood(`
- L57: `@mutation fn save( -> mutation save(`
- L73: `@mutation fn save( -> mutation save(`

### crates/vox-codegen/tests/main_boot_hir_roundtrip.rs (1)
- L21: `@server -> server`

### crates/vox-codegen/tests/main_boot_snapshot.rs (1)
- L31: `@server -> server`

### crates/vox-codegen/tests/property_tests.rs (4)
- L24: `@query fn get_count( -> query get_count(`
- L26: `@mutation fn update_count( -> mutation update_count(`
- L28: `@query fn ping( -> query ping(`
- L29: `@mutation fn pong( -> mutation pong(`

### crates/vox-codegen/tests/push_test.rs (1)
- L19: `@mutation fn store_token( -> mutation store_token(`

### crates/vox-codegen/tests/route_completeness_test.rs (3)
- L19: `@query fn load( -> query load(`
- L35: `@query fn load( -> query load(`
- L52: `@query fn load( -> query load(`

### crates/vox-codegen/tests/tauri_convergence_snapshots.rs (1)
- L25: `@table type Task { -> table Task {`

### crates/vox-codegen/tests/tauri_endpoint_client_parity_test.rs (1)
- L13: `@query fn get_count( -> query get_count(`

### crates/vox-codegen/tests/webhook_emit_test.rs (1)
- L19: `@server (leading) -> server (leave @webhook unchanged) in src literal`

### crates/vox-codegen/tests/webir_contract_parity_test.rs (6)
- L47: `@server -> server`
- L50: `@query -> query`
- L53: `@mutation -> mutation`
- L78: `"@server\nfn ping(x: Int) -> Int { x }" -> "server fn ping(x: Int) -> Int { x }" (replace @server with server)`
- L88: `@query\nfn list_items... -> query (replace @query with query in src literal)`
- L98: `@mutation\nfn delete_item... -> mutation (replace @mutation with mutation in src literal)`

### crates/vox-compiler/benches/compiler_pipeline.rs (5)
- L26: `@table type Task { -> table Task {`
- L32: `@query\nfn list_tasks() to list[Task] { -> query list_tasks() to list[Task] {`
- L37: `@mutation\nfn create_task(title: str) to Task { -> mutation create_task(title: str) to Task {`
- L42: `@mutation\nfn complete_task(id: int) to Task { -> mutation complete_task(id: int) to Task {`
- L47: `@mutation\nfn delete_task(id: int) to Unit { -> mutation delete_task(id: int) to Unit {`

### crates/vox-compiler/src/app_contract.rs (11)
- L221: `@table type Task { title: str done: bool } -> table Task { title: str done: bool }`
- L223: `@server fn sf_one(a: int) to str { return "x" } -> server sf_one(a: int) to str { return "x" }`
- L224: `@server fn sf_two() to int { return 0 } -> server sf_two() to int { return 0 }`
- L225: `@query fn q_one(n: int) to int { return n } -> query q_one(n: int) to int { return n }`
- L226: `@mutation fn m_one(title: str) to int { -> mutation m_one(title: str) to int {`
- L230: `@mutation fn m_two(x: bool) to bool { return x } -> mutation m_two(x: bool) to bool { return x }`
- L272: `@mutation fn m_only(x: int) to int { return x } -> mutation m_only(x: int) to int { return x }`
- L291: `@server fn a(x: int) to str { return "a" } -> server a(x: int) to str { return "a" }`
- L292: `@mutation fn b(y: bool) to bool { return y } -> mutation b(y: bool) to bool { return y }`
- L293: `@query fn c(n: int) to int { return n } -> query c(n: int) to int { return n }`
- L294: `@server fn d() to int { return 0 } -> server d() to int { return 0 }`

### crates/vox-compiler/src/fmt/mod.rs (5)
- L101: `@table type Note {\n title: str ...} -> table Note {\n title: str ...}`
- L112: `@server fn greet(name: str) to str { -> server greet(name: str) to str {`
- L121: `@query fn list_items() to list[Item] { -> query list_items() to list[Item] {`
- L130: `@mutation fn add_item(name: str) to Result[str] { -> mutation add_item(name: str) to Result[str] {`
- L191: `@table type User {\n name: str\n age: int\n} -> table User {\n name: str\n age: int\n}`

### crates/vox-compiler/src/hir/lower/mod.rs (10)
- L813: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L842: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L870: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L895: `@table(pk: email) type User { email: str active: bool } -> table(pk: email) User { email: str active: bool }`
- L919: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L954: `@table type User { name: str age: int active: bool } -> table User { name: str age: int active: bool }`
- L982: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L1017: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L1018: `@query fn q1() to int { return 0 } -> query q1() to int { return 0 }`
- L1019: `@mutation fn m1() to Unit { -> mutation m1() to Unit {`

### crates/vox-compiler/src/parser/descent/tests.rs (4)
- L632: `@table type Task { title: str\n done: bool\n priority: int } -> table Task { title: str\n done: bool\n priority: int }`
- L646: `@table(extern, source: "legacy_users", pk: user_id) type User { user_id: int\n name: str } -> table(extern, source: "legacy_users", pk: user_id) User { user_id: int\n name: str }`
- L671: `@index Task.by_done on (done, priority) -> index Task.by_done on (done, priority)`
- L712: `@server fn echo(x: str) to str {\n    return x\n} -> server echo(x: str) to str {\n    return x\n}`

### crates/vox-compiler/src/typeck/placement.rs (2)
- L458: `@query fn list() { 0 } -> query list() { 0 }`
- L530: `@query fn list_tasks() { 0 } -> query list_tasks() { 0 }`

### crates/vox-compiler/tests/async_handler_test.rs (2)
- L9: `@query fn slow_fetch() to int { -> query slow_fetch() to int {`
- L36: `@query fn slow_fetch() to int { -> query slow_fetch() to int {`

### crates/vox-compiler/tests/at_public_parse_test.rs (4)
- L19: `@public @server\nfn get_health() -> Str { "ok" }\n -> @public server get_health() -> Str { "ok" }\n`
- L23: `@public @query\nfn list_products(limit: Int) -> Str { "[]" }\n -> @public query list_products(limit: Int) -> Str { "[]" }\n`
- L29: `@public @mutation\nfn create_session(token: Str) -> Str { "ok" }\n -> @public mutation create_session(token: Str) -> Str { "ok" }\n`
- L41: `@public @server\nfn get_health() -> Str { "ok" }\n -> @public server get_health() -> Str { "ok" }\n`

### crates/vox-compiler/tests/auth_decorator_test.rs (3)
- L36: `@server @auth(provider: "clerk")\nfn admin() -> Str { "ok" }\n -> @auth(provider: "clerk") server admin() -> Str { "ok" }\n (move @auth before keyword 'server')`
- L46: `@server @auth(provider: "clerk", roles: ["admin", "editor"])\nfn admin_only() -> Str { "ok" }\n -> @auth(provider: "clerk", roles: ["admin", "editor"]) server admin_only() -> Str { "ok" }\n`
- L68: `@server @auth(provider: "supabase")\nfn api() -> Str { "ok" }\n -> @auth(provider: "supabase") server api() -> Str { "ok" }\n`

### crates/vox-compiler/tests/axum_emit_contract_test.rs (5)
- L43: `@query fn q_ping() to int { return 1 } -> query q_ping() to int { return 1 }`
- L71: `@table type T { a: str } -> table T { a: str }`
- L73: `@mutation fn m_save() to int { -> mutation m_save() to int { (insta snapshot may need rebaselining)`
- L86: `@query fn q_only() to int { return 1 } -> query q_only() to int { return 1 }`
- L104: `@server fn sf_ping() to int { return 1 } -> server sf_ping() to int { return 1 }`

### crates/vox-compiler/tests/bug_d_imports_repro.rs (2)
- L8: `@query\nfn parse_voice(s: str) -> str { -> query parse_voice(s: str) -> str {`
- L13: `@mutation\nfn record_event(name: str, payload: str) -> str { -> mutation record_event(name: str, payload: str) -> str {`

### crates/vox-compiler/tests/db_query_safety_test.rs (11)
- L14: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L15: `@query fn q() to int { -> query q() to int {`
- L31: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L32: `@query fn q() to int { -> query q() to int {`
- L48: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L64: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L79: `@table type User { name: str active: bool } -> table User { name: str active: bool }`
- L94: `@table(pk: email) type User { name: str active: bool } -> table(pk: email) User { name: str active: bool }`
- L107: `@table(pk: email) type User { email: Option[str] name: str } -> table(pk: email) User { email: Option[str] name: str }`
- L120: `@table(pk: email) type User { email: str name: str } -> table(pk: email) User { email: str name: str }`
- L138: `@table type User { name: str active: bool } -> table User { name: str active: bool }`

### crates/vox-compiler/tests/form_hir_test.rs (2)
- L8: `@mutation fn save_x(s: int, n: str) to int { return 1 } -> mutation save_x(s: int, n: str) to int { return 1 }`
- L25: `@mutation fn save_x(s: str) to int { return 1 } -> mutation save_x(s: str) to int { return 1 }`

### crates/vox-compiler/tests/ga_boilerplate_grafts_test.rs (10)
- L196: `@server\n@webhook(provider: custom)\nfn custom_hook() to int { return 1 } -> @webhook(provider: custom)\nserver custom_hook() to int { return 1 }`
- L214: `@server\n@webhook(provider: custom, secret: "WEBHOOK_SECRET")\nfn custom_hook() to int { return 1 } -> @webhook(provider: custom, secret: "WEBHOOK_SECRET")\nserver custom_hook() to int { return 1 }`
- L237: `@server\n@webhook(provider: stripe, replay_window_secs: 4)\nfn tight_hook() to int { return 1 } -> @webhook(provider: stripe, replay_window_secs: 4)\nserver tight_hook() to int { return 1 }`
- L256: `@server\n@webhook(provider: stripe)\nfn stripe_hook() to int { return 1 } -> @webhook(provider: stripe)\nserver stripe_hook() to int { return 1 }`
- L279: `@server\n@cors(origins: ["*"], allow_credentials: true)\nfn my_api() to int { return 1 } -> @cors(origins: ["*"], allow_credentials: true)\nserver my_api() to int { return 1 }`
- L298: `@server\n@cors(origins: ["https://app.example.com"], allow_credentials: true)\nfn my_api() to int { return 1 } -> @cors(...)\nserver my_api() to int { return 1 }`
- L316: `@server\n@pii(class: email)\nfn send_email_fn() to int { return 1 } -> @pii(class: email)\nserver send_email_fn() to int { return 1 }`
- L335: `@server\n@pii(class: email)\n@uses(net)\nfn send_email_fn() to int { return 1 } -> @pii(class: email)\n@uses(net)\nserver send_email_fn() to int { return 1 }`
- L400: `@server\n@layer(tier: system_overlay)\nfn debug_fn() to int { return 1 } -> @layer(tier: system_overlay)\nserver debug_fn() to int { return 1 }`
- L419: `@server\n@layer(tier: modal)\nfn confirm_fn() to int { return 1 } -> @layer(tier: modal)\nserver confirm_fn() to int { return 1 }`

### crates/vox-compiler/tests/interpreter_db_test.rs (9)
- L35: `@table type User { name: str, age: int } -> table User { name: str, age: int }`
- L51: `@table type Item { name: str } -> table Item { name: str }`
- L67: `@table type User { name: str, age: int } -> table User { name: str, age: int }`
- L83: `@table type User { name: str, active: bool } -> table User { name: str, active: bool }`
- L99: `@table type Item { name: str } -> table Item { name: str }`
- L124: `@table type User { name: str, age: int } -> table User { name: str, age: int }`
- L145: `@table type User { name: str, age: int } -> table User { name: str, age: int }`
- L167: `@table type User { name: str, age: int } -> table User { name: str, age: int }`
- L188: `@table type User { name: str } -> table User { name: str }`

### crates/vox-compiler/tests/ir_emission_test.rs (1)
- L10: `@server\n        fn greet(name: str) { -> server greet(name: str) {`

### crates/vox-compiler/tests/keyword_decorator_equivalence.rs (1)
- L153: `@query fn g() to int { return len(db.query()) } -> query g() to int { return len(db.query()) }`

### crates/vox-compiler/tests/match_exhaustiveness_test.rs (2)
- L60: `@table type Task { -> table Task {`
- L64: `@query fn active() to int { -> query active() to int {`

### crates/vox-compiler/tests/projection_parity_test.rs (9)
- L23: `@table type Task { title: str done: bool } -> table Task { title: str done: bool }`
- L34: `@query fn ping() to int { return 1 } -> query ping() to int { return 1 }`
- L35: `@server fn sf_ping() to int { return 1 } -> server sf_ping() to int { return 1 }`
- L36: `@query fn list_tasks() to int { return 0 } -> query list_tasks() to int { return 0 }`
- L37: `@mutation fn save_task(title: str) to int { -> mutation save_task(title: str) to int {`
- L90: `@query fn on_back() to bool { return true } -> query on_back() to bool { return true }`
- L115: `@query @uses(net) fn api_ping() to int { return 1 } -> query @uses(net) api_ping() to int { return 1 } (verify @query @uses ordering)`
- L117: `@query fn handle_link(url: str) to str { return "/" } -> query handle_link(url: str) to str { return "/" }`
- L118: `@mutation fn store_token(token: str) to str { return token } -> mutation store_token(token: str) to str { return token }`

### crates/vox-compiler/tests/required_capabilities_test.rs (4)
- L24: `@query fn ping() uses net to int { return 1 } -> query ping() uses net to int { return 1 }`
- L36: `@mutation fn store_token(token: str) to str { return token } -> mutation store_token(token: str) to str { return token }`
- L47: `@query fn handle_link(url: str) to str { return "/" } -> query handle_link(url: str) to str { return "/" }`
- L48: `@mutation fn store_token(token: str) to str { return token } -> mutation store_token(token: str) to str { return token }`

### crates/vox-compiler/tests/runtime_projection_smoke_test.rs (2)
- L40: `@table type T { a: str } -> table T { a: str }`
- L58: `@table type T { a: str } -> table T { a: str }`

### crates/vox-compiler/tests/shell_projection_smoke_test.rs (1)
- L16: `@query fn on_back() to bool { return true } -> query on_back() to bool { return true }`

### crates/vox-compiler/tests/voxdb_schema_hir_parity_test.rs (2)
- L9: `@table type User { -> table User {`
- L14: `@index User.user_name on (name) -> index User.user_name on (name)`

### crates/vox-compiler/tests/web_ir_lower_emit_test.rs (7)
- L193: `@query fn load_child() to int { return 1 } -> query load_child() to int { return 1 }`
- L273: `@server fn api_x() to int { -> server api_x() to int { (test is #[ignore]d but still compiled)`
- L276: `@query fn q_list() to int { return 0 } -> query q_list() to int { return 0 }`
- L612: `@server fn api_ping() to int { return 1 } -> server api_ping() to int { return 1 }`
- L614: `@server fn do_work() to int { return 0 } -> server do_work() to int { return 0 }`
- L616: `@query fn read_q() to int { return 0 } -> query read_q() to int { return 0 }`
- L618: `@mutation fn write_m(x: int) to int { return x } -> mutation write_m(x: int) to int { return x }`

### crates/vox-corpus/src/codegen_vox/part_02.rs (6)
- L194: `"@table type {type_name} {{\n{fields}\n}}" -> "table {type_name} {{\n{fields}\n}}"`
- L208: `"@tool\nfn {name}(...) to str {{ ... }}" -> "tool {name}(...) to str {{ ... }}"`
- L220: `"@query\nfn get_{noun}(id: int) to str {{ ... }}" -> "query get_{noun}(id: int) to str {{ ... }}"`
- L226: `"@mutation\nfn update_{noun}(id: int, value: str) to Unit {{ ... }}" -> "mutation update_{noun}(id: int, value: str) to Unit {{ ... }}"`
- L278: `"@server\nfn {name}({params}) to {ret_type} {{ ... }}" -> "server {name}({params}) to {ret_type} {{ ... }}"`
- L305: `"@index {type_name}.by_{f} on ({f})" -> "index {type_name}.by_{f} on ({f})"`

### crates/vox-corpus/src/codegen_vox/part_03.rs (1)
- L15: `"@table type {tn} {{...}}" -> "table {tn} {{...}}"; "@query\nfn get_{noun}(...)" -> "query get_{noun}(...)"; "@mutation\nfn create_{noun}(...)" -> "mutation create_{noun}(...)" (also @get/@test out-of-scope, leave)`

### crates/vox-corpus/src/corpus/extract_vox/part_walk.rs (1)
- L194: `"@table type AgentMemory {" -> "table AgentMemory {"`

### crates/vox-corpus/src/rust_to_vox.rs (1)
- L69: `format!("@table type {name} {{...}}") -> format!("table {name} {{...}}")`

### crates/vox-corpus/src/synthetic_gen/bodies/_web_pairs.inc (3)
- L17: `"@query\nfn get_user(id: int) -> Option[User] {...}" -> "query get_user(id: int) -> Option[User] {...}"`
- L22: `"@mutation\nfn update_user(id: int, name: str) -> Result[Unit] {...}" -> "mutation update_user(id: int, name: str) -> Result[Unit] {...}"`
- L32: `"@server\nfn generate_pdf(report: str) -> bytes {...}" -> "server generate_pdf(report: str) -> bytes {...}"`

### crates/vox-ml-cli/src/training/multiturn.rs (2)
- L246: `"@query fn my_query() to str {...}" -> "query my_query() to str {...}" (revisit 'already_decorated' test name/assertion)`
- L272: `"// Authentication patterns\n// ---\n@table type Session {...}\n\nfn my_helper() {}" -> "...table Session {...}..."`
