fix(codegen-ts): detect nested async calls and await their results
scale: ~332 lines across 3 files [L]

- `crates/vox-codegen/src/codegen_ts/hir_emit/async_walker.rs`: Added a new module with `stmt_has_async_call` and `expr_has_async_call` functions. These recursively traverse HIR statements and expressions, respectively, to detect calls to async functions, without crossing lambda boundaries.
- `crates/vox-codegen/src/codegen_ts/hir_emit/mod.rs`:
    - Replaced the shallow `stmt_calls_async_fn` with the new recursive `async_walker::stmt_has_async_call` to accurately identify when a statement contains an async call, including those nested in `let` bindings, assignments, or method chains.
    - Modified `emit_hir_expr` to wrap the result of an `await`ed expression in parentheses when it's the receiver of a method call (e.g., `(await fetch_user()).trim()`) to ensure correct evaluation.
    - Updated the logic for emitting event handlers (`emit_hir_event_handler`) to correctly mark them as `async` and inject `await` keywords when `async_walker::stmt_has_async_call` detects an async call within the handler's body.
- `apps/vox-mental-tracker/tests/e2e/voice_flow.spec.ts`: Re-enabled and updated assertions for the `parse` and `save` actions in the E2E test. This now verifies that nested `@endpoint` calls correctly trigger async handler emission and `await` injection, allowing the `p.kind` and `p.payload` to be read correctly after an async operation.

--- audit: original='fix(codegen-ts): recursive async-call detection covers method/let/index chains' | actual=332L across 3 files | derived=full ---
