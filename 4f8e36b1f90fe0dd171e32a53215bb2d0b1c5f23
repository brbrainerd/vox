feat(codegen-ts): add web behavioral gate for emitted React components
scale: ~350 lines across 2 files [L]

- `crates/vox-integration-tests/tests`: Add `ts_emit_behavioral_test.rs` to verify the runtime behavior of generated TypeScript/React components. This test:
    - Emits TSX from `golden-ts` component fixtures using the codegen pipeline.
    - Transpiles the TSX to ESM JavaScript using `tsc`.
    - Renders the component using `react-dom/server`'s `renderToStaticMarkup`.
    - Asserts the output is non-empty and contains expected elements/text, catching runtime bugs that `tsc --noEmit` misses.
    - Includes initial seed fixtures: `component_state` (Counter) and `component_effect_with_deps` (Timer).
    - Is marked `#[ignore]` by default, requiring Node.js and `node_modules` to be installed.
- `crates/vox-integration-tests/ts-noemit-scratch`: Update `.gitignore` to ignore the `__behavioral_test__/` directory created by the new test.

--- audit: original='test(codegen-ts): CR-F2b web behavioral gate seed (render emitted React)' | actual=350L across 2 files | derived=full ---
