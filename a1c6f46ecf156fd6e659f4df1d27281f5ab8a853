feat(compiler): wire `@reactive` cross-call dep inference into reactive emit
scale: ~81 lines across 3 files [M]

- `crates/vox-compiler/src/codegen_ts/reactive.rs`:
  - `generate_reactive_component` now collects `@reactive`-annotated free functions from the module into a `HashMap` (name -> body).
  - This `HashMap` is passed to `extract_state_deps_with_callees` when calculating dependencies for `derived` and `effect` members, enabling cross-call dependency inference.
  - Added two new end-to-end unit tests to verify this behavior.
- `crates/vox-compiler/src/codegen_ts/hir_emit/mod.rs`:
  - Re-exports `extract_state_deps_with_callees` from `state_deps` module.
- `crates/vox-compiler/src/parser/descent/mod.rs`:
  - Updated top-level declaration dispatch logic to recognize `Token::AtReactive` for functions at module scope, allowing `@reactive fn name() { ... }` to parse correctly.

--- audit: original='feat(compiler): wire @reactive cross-call dep inference into reactive emit' | actual=81L across 3 files | derived=full ---
