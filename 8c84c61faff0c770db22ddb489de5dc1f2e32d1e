docs(adr): establish state_machine as SSoT and deprecate vox-vscode
scale: ~123 lines across 6 files [M]

- docs/src/adr/031-deprecate-vox-vscode.md: Add new ADR formally deprecating `vox-vscode` as the primary user surface, designating `vox-dashboard` as the replacement, and retaining `vox-vscode` for LSP only.
- docs/src/adr/030-state-machine-ssot.md: Add new ADR formally adopting `state_machine` as the Single Source of Truth for reactive UI state in `vox-dashboard` and Vox-generated applications, including a CI gate requirement.
- docs/src/reference/vox-web-stack.md: Update "Canonical Frontend" section to declare `vox-dashboard` as the SSoT and `vox-vscode` as deprecated, add details on `vox-dashboard` entry points and backend, and remove outdated references.
- docs/src/SUMMARY.md: Register ADR 030 and ADR 031 in the documentation summary.
- docs/src/adr/010-tanstack-web-spine.md: Add a note clarifying that this ADR applies to Vox-compiled user applications, not `vox-dashboard`.
- docs/src/adr/027-dual-track-ui-surfaces.md: Add an amendment note pointing to ADR 031 regarding the deprecation of `vox-vscode` and the primary role of `vox-dashboard`.

--- audit: original='docs(adr): establish state_machine SSoT and deprecate vox-vscode' | actual=123L across 6 files | derived=full ---
