feat(audit): Achieve 100% plan fidelity by fixing Vox syntax and test gating
scale: ~202 lines across 7 files [M]

- `crates/vox-audit`: Added `tests_required` field to `PlanFixture` to gate test execution based on `plan.toml` criteria, preventing refactor-only plans from failing due to missing tests. Increased `DEFAULT_MAX_ITERATIONS` from 3 to 7.
- `contracts/eval/plan-fidelity/plans`:
    - `004-refactor-loop/base.vox`: Rewrote `TodoError` enum to Vox ADT syntax (`type X = | Empty(text: str)`), replaced `s.len()` with `len(s)`, and `Err(TodoError.Empty)` with `Error(Empty("..."))`.
    - `005-implement-endpoint/base.vox`: Rewrote `TodoError` enum to Vox ADT syntax, replaced `s.len()` with `len(s)`, and `Err(...)` with `Error(...)`. Updated test to use `match` for `Result` variants.
    - `004-refactor-loop/plan.toml`: Updated prompt to explicitly declare Vox conventions for `len()`, `Error(...)`, `Ok(...)`, and bare variant constructors.
    - `005-implement-endpoint/plan.toml`: Updated prompt to explicitly declare Vox conventions for `len()`, `Error(...)`, `Ok(...)`, and bare variant constructors, and refined error message examples.
- `contracts/reports`: Updated `plan-fidelity/2026-05-23.json` to reflect 100% pass rate and updated `evidence-ledger.v1.json` notes to document the trajectory to 100% fidelity.

--- audit: original='feat(audit): CR-L4 70% → 100% — Vox-syntax base sources + per-plan test gate' | actual=202L across 7 files | derived=full ---
